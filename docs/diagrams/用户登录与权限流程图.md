# 用户登录与权限流程图

**项目名称**：基于 DeepSeek 的西藏旅游景点智能评价与分析系统
**文档版本**：V1.0
**编制日期**：2026-09-20
**配套文档**：`docs/design/详细设计说明书.md` §13
**对应 C4 组件**：`C-API-01` 用户认证组件、`C-API-14` 管理组件
**对应需求**：FR-SY-01、FR-SY-02、NR-S-01、NR-S-02

---

## 1. 注册流程

```mermaid
flowchart TD
    A["用户提交注册<br/>用户名 + 密码"] --> B{"参数校验<br/>用户名 3–32 字符？<br/>密码 ≥6 位？"}
    B -->|否| B1["返回 1001/1002<br/>提示具体错误"]
    B -->|是| C{"用户名是否已存在？"}
    C -->|是| C1["返回 2003<br/>提示：用户名已存在"]
    C -->|否| D["密码加盐哈希<br/>（PBKDF2 / bcrypt）"]
    D --> E["写入 sys_user<br/>role 默认 = 'user'"]
    E --> F["返回注册成功<br/>（不自动登录，跳转登录页）"]

    classDef check fill:#FCE4EC,stroke:#C2185B,color:#7B1039
    classDef ok fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef warn fill:#FFEBEE,stroke:#C62828,color:#8B1A1A
    classDef proc fill:#FFFFFF,stroke:#8496B0,color:#333333

    class B,C check
    class D,E proc
    class F ok
    class B1,C1 warn
```

---

## 2. 登录与权限校验流程

```mermaid
flowchart TD
    A["用户访问系统"] --> B{"是否已登录<br/>（会话有效）？"}
    B -->|否| C["以游客身份浏览<br/>（只读，无问答历史）"]
    B -->|是| D["读取会话中的 user_id 与 role"]

    C --> E{"访问的是否为<br/>需登录的接口？"}
    D --> E
    E -->|否| F["放行：公共只读接口"]
    E -->|是| G{"会话是否有效？"}
    G -->|否| G1["返回 2001 未登录<br/>跳转登录页"]
    G -->|是| H{"该接口是否要求管理员？"}

    H -->|否| I["放行：用户级接口<br/>（问答历史、提问）"]
    H -->|是| J{"role == 'admin'？"}
    J -->|否| J1["返回 2002 权限不足"]
    J -->|是| K["放行：管理接口"]

    L["用户提交登录<br/>用户名 + 密码"] --> M["查询 sys_user"]
    M --> N{"用户存在且状态正常？"}
    N -->|否| N1["返回 2004<br/>用户名或密码错误<br/>（不区分具体原因，防枚举）"]
    N -->|是| O["校验密码哈希"]
    O --> P{"校验通过？"}
    P -->|否| N1
    P -->|是| Q["创建服务端会话<br/>写入 user_id / role"]
    Q --> R["更新 last_login_at"]
    R --> S["返回登录成功 + 角色"]

    classDef check fill:#FCE4EC,stroke:#C2185B,color:#7B1039
    classDef ok fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef warn fill:#FFEBEE,stroke:#C62828,color:#8B1A1A
    classDef proc fill:#FFFFFF,stroke:#8496B0,color:#333333
    classDef guest fill:#E3F2FD,stroke:#1565C0,color:#0D3C61

    class B,E,G,H,J,N,P check
    class F,I,K,Q,R,S proc
    class C guest
    class D,M,O proc
    class G1,J1,N1 warn
```

---

## 3. 角色与权限矩阵

| 功能 | 游客（未登录） | 普通用户（user） | 管理员（admin） |
|---|---|---|---|
| 数据总览 | ✅ | ✅ | ✅ |
| 景点分析（详情／情感／方面／主题） | ✅ | ✅ | ✅ |
| 景点智能评价（查看） | ✅ | ✅ | ✅ |
| 景点对比 | ✅ | ✅ | ✅ |
| 智能问答（提问） | ✅ | ✅ | ✅ |
| 问答历史查看 | ❌ | ✅ | ✅ |
| 智能评价重新生成 | ❌ | ❌ | ✅ |
| 触发／监控离线任务 | ❌ | ❌ | ✅ |
| 任务日志查询 | ❌ | ❌ | ✅ |
| 用户管理 | ❌ | ❌ | ✅ |
| 数据口径配置查看 | ❌ | ❌ | ✅ |

**权限模型说明（从简）**：

- 仅区分上述三类角色，用 `sys_user.role` 单字段表达；
- **不建权限表、不做菜单级权限、不做细粒度数据权限、不做单点登录**（业务需求范围外事项第 12 项）；
- 管理员权限与普通用户权限为**并列**关系，不是继承关系（业务需求 §4.1）。

---

## 4. 安全设计要点

| # | 要点 | 说明 | 对应需求 |
|---|---|---|---|
| 1 | 密码加盐哈希存储 | 禁止明文；使用 PBKDF2／bcrypt | NR-S-01 |
| 2 | 登录失败不区分原因 | 统一提示"用户名或密码错误"，防止用户名枚举 | — |
| 3 | 会话管理 | 登录写入服务端会话；注销即失效；不实现"记住我"长期令牌 | — |
| 4 | 接口级鉴权 | 需登录接口校验会话；管理接口额外校验角色 | NR-S-02 |
| 5 | **越权防护（重点）** | 问答历史查询**强制以当前会话 `user_id` 过滤**，不接受前端传入他人 ID | — |
| 6 | SQL 注入防护 | 全部使用参数化查询 | NR-S-03 |
| 7 | 错误信息克制 | 不向前端返回堆栈或 SQL 语句 | NR-S-03 延伸 |
| 8 | 口令强度 | 注册时校验最小长度（≥6 位）；不做复杂度强制，避免影响演示 | — |

---

## 5. 会话与游客标识

```mermaid
sequenceDiagram
    participant U as 用户浏览器
    participant API as Flask 应用
    participant DB as MySQL

    Note over U,API: 游客访问（登录可选）
    U->>API: GET /api/overview/summary
    API->>DB: 查询 stat_overview
    DB-->>API: 数据
    API-->>U: 返回（无需登录）

    Note over U,API: 游客提问（可选记录）
    U->>API: POST /api/qa/ask
    API->>API: user_id = NULL，生成 session_key
    API->>DB: INSERT qa_record(user_id=NULL, session_key=...)
    API-->>U: 返回回答（不提供历史查询）

    Note over U,API: 用户登录后提问
    U->>API: POST /api/auth/login
    API->>DB: 查询并校验密码哈希
    DB-->>API: 用户信息
    API->>API: 创建会话（user_id, role）
    API-->>U: 登录成功 + Set-Cookie

    U->>API: POST /api/qa/ask
    API->>API: user_id = 会话中的 user_id
    API->>DB: INSERT qa_record(user_id=...)
    API-->>U: 返回回答

    U->>API: GET /api/qa/history
    API->>DB: SELECT ... WHERE user_id = 当前会话用户
    DB-->>API: 该用户的历史记录
    API-->>U: 返回历史
```

---

## 6. 注销流程

| 步骤 | 动作 |
|---|---|
| 1 | 前端调用 `POST /api/auth/logout` |
| 2 | 服务端销毁会话 |
| 3 | 前端跳转首页，回到游客态 |
| 4 | 已保存的问答记录**保留**（属于历史数据，不随注销删除） |

---

**文档结束**
