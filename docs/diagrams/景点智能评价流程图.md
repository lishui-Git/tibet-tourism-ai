# 景点智能评价流程图

**项目名称**：基于 DeepSeek 的西藏旅游景点智能评价与分析系统
**文档版本**：V1.0
**编制日期**：2026-09-20
**配套文档**：`docs/design/详细设计说明书.md` §15.B、§15.C
**对应 C4 组件**：`C-BAT-06` 事实包构造、`C-BAT-07` 评价生成、`C-API-09` 智能评价组件

---

## 1. 总体流程（离线生成 + 在线读取）

```mermaid
flowchart TD
    A["触发方式<br/>① 管理员手动触发批量任务<br/>② 单景点重新生成（管理员）"] --> B["查询评论量 ≥100 条的景点列表"]
    B --> B1["结果：57 个景点<br/>合计 50,562 条评论（占 85.7%）"]
    B1 --> C{"该景点是否已有<br/>同版本事实包的评价结果？"}
    C -->|是 且 非强制重新生成| C1["跳过（幂等，不重复调用模型）<br/>FR-IE-05 / BR-12"]
    C -->|否 或 强制重新生成| D["构造景点事实包"]

    D --> D1["① 景点基础信息<br/>名称 / 地址 / 介绍摘要"]
    D --> D2["② 评论统计指标<br/>评论量 / 均分 / 好评率 / 差评率 / 图文率"]
    D --> D3["③ 情感分析结果<br/>正面 / 中性 / 负面占比 + 样本量"]
    D --> D4["④ 方面分析结果<br/>各方面样本量与倾向<br/>（样本<10 条不纳入，BR-04）"]
    D --> D5["⑤ 代表性正面评论<br/>2–5 条原文"]
    D --> D6["⑥ 代表性负面评论<br/>2–5 条原文"]

    D1 --> E["事实包组装（JSON）"]
    D2 --> E
    D3 --> E
    D4 --> E
    D5 --> E
    D6 --> E
    E --> F["写入 spot_fact_package<br/>保存快照（版本号 v1）<br/>FR-IE-07"]

    F --> G["组装 Prompt<br/>硬性约束 + 事实包 JSON<br/>temperature = 0.3"]
    G --> H["调用 DeepSeek<br/>max_tokens = 1200"]
    H --> I{"请求成功且可解析？"}
    I -->|否| I1{"重试次数 < 2？"}
    I1 -->|是| H
    I1 -->|否| I2["记录失败<br/>不写入 spot_report<br/>前端保持不可用并提示"]

    I -->|是| J["内容校验"]
    J --> J1["四段字段是否齐全<br/>summary / advantages / issues / visitor_focus"]
    J1 --> J2["条目数量 2–6 条<br/>超量截断，少量保留不编造"]
    J2 --> J3["数字一致性校验<br/>抽取回答中的数字与事实包比对"]

    J3 --> K{"存在与事实包不符的数字？"}
    K -->|是| K1["标记 need_review = 1<br/>仍入库，前端标注待人工复核"]
    K -->|否| K2["正常入库"]
    K1 --> L["写入 spot_report<br/>关联 fact_package_version / task_id"]
    K2 --> L

    L --> M["在线：前端 GET /api/spots/{id}/report"]
    M --> N["读取 spot_report + 事实包摘要"]
    N --> O["返回评价内容 + 评价所依据的关键数据<br/>FR-IE-06"]
    O --> P["页面展示：评价四段 + 依据回显"]

    classDef check fill:#FCE4EC,stroke:#C2185B,color:#7B1039
    classDef ai fill:#FFF2CC,stroke:#BF8F00,color:#7F6000
    classDef store fill:#E2EFDA,stroke:#548235,color:#375623
    classDef proc fill:#FFFFFF,stroke:#8496B0,color:#333333
    classDef warn fill:#FFEBEE,stroke:#C62828,color:#8B1A1A

    class C,I,K check
    class G,H ai
    class D,D1,D2,D3,D4,D5,D6,E,J,J1,J2,J3,K1,K2,M,N,O,P proc
    class F,L store
    class I2 warn
```

---

## 2. 事实包组成（唯一的评价依据）

```mermaid
flowchart LR
    subgraph src["数据来源"]
        S1["spot 表<br/>基础信息"]
        S2["stat_spot 表<br/>统计指标"]
        S3["sentiment 表<br/>情感占比"]
        S4["aspect 表<br/>方面分析"]
        S5["review 表<br/>评论原文"]
    end
    subgraph pkg["📦 景点事实包（JSON 快照）"]
        P1["basic<br/>名称/地址/介绍摘要"]
        P2["statistics<br/>评论量/均分/好评率/差评率/图文率"]
        P3["sentiment<br/>正面/中性/负面 + 样本量"]
        P4["aspects<br/>各方面样本量与倾向"]
        P5["representative_reviews<br/>正负面原文各 2–5 条"]
        P6["caliber<br/>门槛口径说明"]
    end
    S1 --> P1
    S2 --> P2
    S3 --> P3
    S4 --> P4
    S5 --> P5
    P1 & P2 & P3 & P4 & P5 --> P6
```

### 代表性评论筛选规则

| 顺序 | 规则 |
|---|---|
| 1 | 排除低信息量（正文 ≤10 字） |
| 2 | **优先选取非重复正文**（避免"值得一去"这类撞车短句） |
| 3 | 按点赞数降序；正文长度 30–200 字优先 |
| 4 | 正面取 `score ≥4`、负面取 `score ≤2` |
| 5 | 各取 2–5 条；**负面不足则如实留少，不凑数** |

---

## 3. Prompt 约束（BR-07 的实现）

```
[system]
你是旅游景点评论分析报告的撰写者。你只能使用"事实数据"中给出的信息撰写报告。
【硬性约束】
1. 不得使用你自身的知识补充任何事实。
2. 引用数字必须与事实数据完全一致，不得四舍五入后改变量级。
3. 事实数据中未涉及的方面，不要提及。
4. 不得给出绝对的推荐结论，只陈述数据反映的情况。
5. 输出 JSON，字段为 summary / advantages / issues / visitor_focus。
```

---

## 4. 门槛与降级（重要）

```mermaid
flowchart TD
    Q["用户进入景点详情页"] --> R{"评论量 ≥ 100 条？"}
    R -->|否| S1["available = false<br/>reason = REVIEW_COUNT_BELOW_THRESHOLD"]
    S1 --> S2["页面提示：<br/>该景点评论样本不足，仅提供基础统计"]
    S2 --> S3["仅展示：基础信息 + 统计指标<br/>隐藏情感、方面、评价等评价性内容"]
    R -->|是| T{"spot_report 是否已存在？"}
    T -->|是| U1["available = true<br/>返回评价 + 依据"]
    T -->|否| U2["返回 available = false<br/>reason = REPORT_NOT_GENERATED"]
    U2 --> U3["提示：智能评价尚未生成<br/>（管理员可触发生成）"]

    classDef check fill:#FCE4EC,stroke:#C2185B,color:#7B1039
    classDef ok fill:#E8F5E9,stroke:#2E7D32,color:#1B5E20
    classDef warn fill:#FFF8E1,stroke:#F9A825,color:#7A5A00

    class R,T check
    class U1 ok
    class S1,S2,S3,U2,U3 warn
```

| 情况 | 系统行为 | 对应需求 |
|---|---|---|
| 评论量 ≥100 且已有报告 | 展示完整评价 ＋ 依据数据 | FR-IE-03、FR-IE-06 |
| 评论量 ≥100 但未生成 | `available=false`，提示尚未生成 | FR-IE-09 |
| 评论量 <100 | `available=false`，只提供基础统计并提示样本不足 | BR-02、BR-03、FR-SA-09 |
| 方面样本 <10 条 | 该方面不纳入事实包，不参与评价 | BR-04 |
| 数字校验不通过 | 入库但标记 `need_review=1`，前端标注"待人工复核" | FR-IE-04 的强制关卡 |
| 生成失败 | 不写库，保持不可用并提示，保留历史结果 | FR-IE-09、NR-R-05 |

---

## 5. 可追溯链

```
spot_report（评价文本）
   ├─ fact_package_version ──▶ spot_fact_package.package_json（当时的数字快照）
   │                              ├─ statistics（评论量/均分/好评率…）
   │                              ├─ sentiment（占比 + 样本量）
   │                              ├─ aspects（各方面样本量）
   │                              └─ representative_reviews（comment_id 列表）
   │                                     └─▶ review.content（原文）
   ├─ model（如 deepseek-chat）
   ├─ prompt_version（Prompt 模板版本）
   ├─ generated_at（生成时间）
   └─ task_id ──▶ analysis_task（哪次任务产生的）
```

**能回答的三个问题**：① 这段评价依据了哪些数据；② 数据何时、由哪次任务算出；③ 用的是哪版事实包与模型。这是 FR-IE-06／07 的完整实现。

---

**文档结束**
