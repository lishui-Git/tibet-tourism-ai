# =============================================================================
#  Dependencies - DeepSeek-based Tibet Tourism Attraction Review System
#
#  NOTE: This file is intentionally ASCII-only (English comments, no BOM).
#  Reason: pip decodes requirements files with the system locale encoding.
#  On Chinese Windows that is GBK, so UTF-8 Chinese comments here trigger
#  "UnicodeDecodeError: 'gbk' codec can't decode byte ...".
#  Project documents are written in Chinese; THIS FILE MUST STAY ASCII.
#
#  Design references (see the docs/ folder):
#    docs/design/详细设计说明书.md  section 2 and 7.2
#    docs/architecture/C4容器图.md   section 2
#
#  Install:  python -m pip install -r requirements.txt
# =============================================================================

# --- Stage 1: minimal runnable Flask app, MySQL connectivity, CSV import ---
Flask==3.1.3
PyMySQL==1.1.1
# MySQL 8 default auth plugin caching_sha2_password requires cryptography.
cryptography>=44.0.0
python-dotenv==1.2.3

# --- Stage 3: Python data cleaning and text preprocessing (pandas + jieba) ---
pandas==2.2.3
jieba==0.42.1

# --- Stage 5 and 6: DeepSeek batch processing and online Q&A ---
# The design calls DeepSeek over HTTPS/REST directly via requests,
# so the openai SDK is deliberately NOT added here.
requests==2.34.2

# =============================================================================
# Spark offline analysis (Stage 4) is implemented in Scala 2.13.10 + Maven
# and is NOT part of the Python dependency set.
# See: docs/architecture/C4容器图.md and spark/README.md
# =============================================================================
