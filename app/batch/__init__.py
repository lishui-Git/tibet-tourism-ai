# -*- coding: utf-8 -*-
"""Python 批处理服务（C4 容器「Python 批处理服务」）。

本包承载 C4 组件 `C-BAT-01` ~ `C-BAT-07`：

    C-BAT-01  任务编排与断点续跑组件   → 后续阶段
    C-BAT-02  数据校验组件             → 阶段一（import_dataset.py 中实现：行数校验）
    C-BAT-03  清洗与规范化组件         → 阶段一（import_dataset.py 中实现：字段映射与派生）
    C-BAT-04  去重与分层组件           → 阶段一（import_dataset.py 中实现：低信息量与重复标记）
    C-BAT-05  语义抽取组件             → 阶段五
    C-BAT-06  事实包构造组件           → 阶段五
    C-BAT-07  评价生成组件             → 阶段五

说明：阶段一按「先建表、再导数据、后清洗分析」的既定顺序（详细设计 §16），
只把 C-BAT-02/03/04 中**导入所必需**的部分实现在 `import_dataset.py`，
尚未实现的组件不建空文件，避免出现「有文件无实现」的假进度。
"""
