# -*- coding: utf-8 -*-
"""Web 路由层（接口层）。

按详细设计说明书 §2 的分层约束：
    · 接口层只做参数校验与响应封装，**不写业务规则**（业务规则集中在业务服务层）；
    · 所有接口统一响应体 `{code, message, data}`（详细设计 §6.3）；
    · `code = 0` 表示成功——**包含「数据不足」与「超范围拒答」两种正常业务响应**
      （详细设计 §6.4），前端不得把 code=0 一律当作「有数据」。

阶段一只实现自检类接口；26 个业务接口按详细设计 §6.2 在后续阶段实现。
"""
