# DSH 开发上下文索引

> 本文件专门用于 DeepSeek Harness（DSH）快速建立项目上下文。
>
> **重要：不要因为本文件存在而扫描整个项目目录。**
> 默认只读取本文件；根据当前任务，按「按需读取规则」打开少量相关文件。
>
> 最后核对时间：以本文件与项目根目录实际文件一致为准（核对基线：`app/`、`docs/`、`scripts/`、`spark/`、`data/`、`README_开发说明.md`、`ReadMe.md`）。

---

## 0. 按需读取规则（DSH 必读）

| 当前任务类型 | 只读这些文件 |
|---|---|
| 写后端接口 / Flask 路由 | `docs/design/详细设计说明书.md` §2、§6.2、§6.3；`app/config.py`、`app/db.py`、`app/web/response.py`、`app/web/routes/` |
| 改数据库相关代码 | `docs/database/数据库设计说明.md`、`docs/database/建表脚本.sql`；`app/db.py` |
| 写 Spark 离线分析 | `docs/diagrams/数据处理流程图.md`、`docs/design/详细设计说明书.md` 中 `C-SPK-*` 组件；`spark/README.md` |
| 接入 DeepSeek | `docs/design/详细设计说明书.md` §7、§15.A/B/C；`docs/diagrams/DeepSeek语义分析流程图.md`、`景点智能评价流程图.md`；`app/config.py` 的 `settings.deepseek`；**`app/llm/README.md`（阶段四实现说明）** |
| 做前端页面 / ECharts | `docs/design/详细设计说明书.md` §「交互」；`app/web/templates/index.html`、`app/web/static/` |
| 核对业务规则（≥100 条门槛、客源地口径等） | `开题/业务需求文档.md`、`docs/设计阶段一致性检查报告.md` |
| 记开发日报 | `开发日报/开发日报.md`（体例说明 + 末尾追加） |
| 全局规范疑问 | `ReadMe.md`（AI 工作守则、开发纪律、日报规则） |

**通用纪律（来自 `ReadMe.md`）**：先看再改；数据文件默认只读；删除/覆盖需先确认；不留垃圾（临时文件放 `tmp/`）；变动留痕；只在项目根目录内操作。

---

## 1. 项目基本信息

**项目名称：**
《基于DeepSeek的西藏旅游景点智能评价与分析系统的设计与实现》

**项目类型：**
毕业设计 / 大数据方向

**当前阶段：**
系统设计已完成，数据库已建立并完成数据导入；阶段一（骨架与导入）、阶段二（Python 清洗）、
阶段三（Spark 离线分析）已完成并 push；阶段四（DeepSeek 语义分析 C-BAT-05～07）代码与
mock 小样本验证已完成，**真实 API 调用待 `.env` 填入 Key 后执行**。

**项目根目录：**
`E:\tibet-tourism-ai`

**数据库：**
MySQL 8.0
数据库名：`tibet_review`

**核心数据：**
- 59,033 条旅游评论
- 837 个景点
- 15 个原始字段
- 西藏景点 + 进藏沿线景点
- 已完成 MySQL 全量导入

---

## 2. 技术栈（已冻结）

### 数据处理
- Python 3.11.7
- pandas
- jieba

### 大数据分析
- Scala 2.13.10
- Spark 3.3.1
- JDK 1.8
- `local[*]`

### AI
- DeepSeek 官方 API
- `deepseek-chat`

### 后端
- Flask
- PyMySQL

### 前端
- HTML
- ECharts
- axios

### 数据库
- MySQL 8.0.32

---

## 3. 系统功能范围

系统包含以下六个核心模块：

- M1 数据总览
- M2 景点分析
- M3 景点智能评价
- M4 景点对比
- M5 智能问答
- M6 系统管理

---

## 4. 数据库现状

当前数据库设计为 **17 张表**。

核心数据表：
- `spot`
- `review`

语义分析：
- `sentiment`（阶段三已写 `mllib` 基线；阶段四将写 `deepseek`）
- `aspect`（阶段四写入，当前为空）
- `comment_semantic`（阶段四写入，当前为空）

主题分析：
- `topic`
- `topic_word`

统计分析：
- `stat_overview`
- `stat_spot`
- `stat_time`
- `stat_ip`

解释与评价：
- `spot_fact_package`（阶段四写入，当前为空）
- `spot_report`（阶段四写入，当前为空）

业务：
- `qa_record`

运维：
- `analysis_task`
- `task_log`

用户：
- `sys_user`

### 已删除/合并的设计

以下内容已经取消，不要重新创建：

- `ml_model`
- `llm_failure`
- `clean_log`
- `spot_comparison`

其中部分能力已经合并到现有表或采用实时计算方式实现。

---

## 5. 当前已经完成的工作

### 数据阶段
- 数据采集完成
- 数据质量检查完成
- 最终数据集确定
- 数据辅助标注文件完成
- 重复内容清单完成
- MySQL 全量导入完成

### 设计阶段
- 需求分析完成
- C4 架构设计完成
- 详细设计完成
- 数据库设计完成
- ER 图完成
- 建表脚本完成
- 流程图完成
- 设计阶段一致性检查完成

### 开发阶段
已经完成基础项目骨架，包括：
```text
app/
├─ __init__.py
├─ config.py                集中配置（唯一读 .env 入口）
├─ db.py                    数据访问（参数化 SQL）
├─ batch/                   Python 批处理（C-BAT-01~07）
│  ├─ import_dataset.py     CSV → spot / review 导入（含 18 项校验，阶段一）
│  ├─ clean_dataset.py      清洗与预处理 SOP（阶段二）
│  ├─ task_registry.py      analysis_task / task_log 登记（阶段四）
│  ├─ semantic_analysis.py  C-BAT-05 评论语义分析（阶段四）
│  ├─ fact_package.py       C-BAT-06 景点事实包（阶段四，零模型调用）
│  └─ spot_report.py        C-BAT-07 景点智能评价（阶段四）
├─ llm/                     DeepSeek 调用封装（阶段四）
│  ├─ client.py             HTTP 调用/重试/限流/超时/用量统计
│  ├─ prompts.py            Prompt 模板与版本号
│  ├─ validators.py         输出校验（枚举/钳制/evidence 原文子串/数字一致性）
│  ├─ mock.py               假客户端（小样本零消耗联调）
│  └─ __main__.py           阶段四统一入口（python -m app.llm --stage ...）
└─ web/
   ├─ __init__.py           create_app() 应用工厂
   ├─ response.py           统一响应体 {code,message,data}
   ├─ routes/
   │  ├─ health.py          /healthz、/api/db-ping
   │  └─ pages.py           /（骨架页）
   ├─ templates/index.html
   └─ static/               css / js

scripts/
├─ check_env.py             环境自检（NR-M-04）
├─ run_dev.ps1              一键：自检 → 启动
├─ verify_cleaned.py        阶段二清洗产物复核
└─ verify_phase4.py         阶段四 29 项自动验证（+ 全量 mock 压测）

spark/                      Spark 离线分析（C-SPK-01~05，阶段三，已实现）
data/                       清洗/导入产物目录（已 gitignore）
logs/                       运行日志目录（已 gitignore）

run.py
requirements.txt
.env.example
.gitignore
README_开发说明.md
开发上下文索引.md
```

已经完成：

- Python 项目结构
- Flask 基础结构
- 数据库连接基础代码
- 数据导入程序
- 基础 Web 页面
- 环境检查脚本
- 开发启动脚本
- Git 仓库初始化
- **阶段二**：数据清洗与预处理（C-BAT-01～04）
- **阶段三**：Spark 离线分析（C-SPK-01～05，全量 59,033 条已运行）
- **阶段四**：DeepSeek 语义分析代码（C-BAT-05～07）＋ 34 项验证通过；
  **真实 API 调用待 `.env` 填入 `APP_DEEPSEEK_API_KEY` 后执行**（详见 `app/llm/README.md`）

### 已验证的硬事实（可直接引用，无需重跑）

- `spot` = 837 行（西藏 554 / 进藏沿线 283），`has_full_evaluation` = 57 个
- `review` = 59,033 行；`analysis_task` 中登记 `task_id=4`、`status=success`、`success_count=59033`
- 17 张表 / 17 主键 / 7 唯一约束 / 14 外键，全部校验通过
- 导入脚本幂等（`ON DUPLICATE KEY UPDATE`），重复执行不产生重复行
- 阶段三：`sentiment`(mllib) 47,110 行、`topic` 265、`topic_word` 2,650、`stat_spot` 837、`stat_time` 3,088、`stat_ip` 3,235
- 阶段四：C-BAT-05 全量调用实测 **47,733 次**（非重复正常评论 47,148 + 重复组代表 585）；
  短评 10,228 条走规则、重复正文 4,239 条/1,285 组只调 585 次（`scripts/verify_phase4.py` 34/34 通过）

---

## 6. 明确未做的事（避免误读进度）

- 完整 REST 接口（详细设计 §6.2 的 26 个接口）未实现，目前只有 2 个自检口
- 5 个业务页面未做（仅有 1 张骨架页）
- ~~DeepSeek 业务逻辑未接入~~ → **代码已完成（阶段四，2026-09-24）**：
  `app/llm/` + `app/batch/semantic_analysis.py` / `fact_package.py` / `spot_report.py`；
  **但尚未做真实 API 调用**（Key 未填），故 `sentiment`(deepseek)/`aspect`/`comment_semantic`/
  `spot_fact_package`/`spot_report` 目前均为 0 行
- ~~Spark 离线分析未开始~~ → **已完成（阶段三，2026-09-23）**
- ~~数据清洗的完整实现（分词、分档统计、清洗后版本文件输出）未做~~ → **已完成（阶段二，2026-09-22）**：`app/batch/clean_dataset.py` 实现 C-BAT-01～04，产出 `data/旅游评论数据集_清洗版_v1.csv` 等三件产物；**分词按下方定论属 Spark 职责，不在 Python 侧实现**
- 14 张结果表中，统计表与情感基线、主题表已由阶段三写入；
  语义结果（deepseek）、事实包、评价报告三组仍待阶段四真实运行后写入
- `spot.poi_url` 未填充（CSV 无此列，`携程西藏景点清单.csv` 仅覆盖 283/837）

### 文本预处理（分词）职责定论 —— 已定，不再讨论

**Python（已完成）**：数据清洗、字段规范化、质量控制、分层标记
**Spark `C-SPK-03`（已完成）**：中文分词、停用词过滤、TF-IDF 及后续文本统计分析

依据：`详细设计说明书.md` §4.8 已把「中文分词／停用词过滤／TF-IDF 向量化」划给 `C-SPK-03`；
`review` 表无分词字段且 17 张表已冻结。**不要在 Python 侧补做分词**，也不要因为
`requirements.txt` 里有 `jieba` 就硬塞功能；`jieba` 依赖保留，供 Spark 侧的 Python 辅助脚本
或后续需要时使用。清洗模块 `app/batch/clean_dataset.py` 保持现状，**不回头修改**。

---

## 7. 冻结与禁止事项（本阶段不要动）

- **不要重新建库、重新采集数据、修改既定架构**
- 数据库 17 张表已冻结，字段名与类型不得随意改动；如需改动，先更新 `docs/database/数据库设计说明.md` 与 `ER图.md`
- C4 组件编号（`C-API-*` / `C-BAT-*` / `C-SPK-*`）是追溯锚点，不实现 C4 中不存在的模块
- `数据采集/旅游评论数据集_最终版.csv`、`景点来源标注.csv`、`内容重复清单.csv`、`最终数据质量报告.md`、`采集工作报告.md` 为**只读冻结区**，清洗/转换必须输出新版本文件
- 不重新运行爬虫，不修改采集阶段代码
- 口令不得写入文档或提交仓库；`.env` 已被 `.gitignore` 排除

---

## 8. 旧路径迁移状态（已修复，仅记录结论）

目录由 `E:\毕业设计` 改为 `E:\tibet-tourism-ai` 后，所有**运行代码与项目文档**中的旧绝对路径已统一替换为新路径，共修复 24 个文件：

- `README_开发说明.md`（2 处）
- `爬虫工具/README_使用说明.md`（2 处）
- `tmp/` 下 22 个脚本（`md2docx.py`、`build_kaiti4.py`、`check_design.py`、`check_sql2.py` 等），其中 `migration_scan.py` 本身也指向新根目录

**附带修复（阻塞性问题，非新功能）**：`scripts/check_env.py` 原先未设置 stdout 编码，在 Windows GBK 控制台输出 `✔` 会抛 `UnicodeEncodeError` 并在第 1 项就中断。已加入 `sys.stdout.reconfigure(encoding="utf-8")`（与 `tmp/` 下脚本一致），现可完整跑通。

已实测复验（均为只读校验，未写库）：
- `scripts/check_env.py` → 6 组检查全绿，`环境就绪，可以继续开发。`（exit 0）
- `tmp/check_sql2.py` → 17 条 CREATE TABLE 括号配平通过
- `tmp/check_ghost.py` → 可正常读文件并输出

复核出的项目硬事实（与第 5 节一致）：17 表 / 17 主键 / 7 唯一 / 14 外键；`spot` 837 行；`review` 59,033 行。

**以下 3 处保留旧路径不动（属历史记录，不是遗漏）：**

- `开发日报/开发日报.md` 第 766 行 —— 按「只追加，不覆盖」规则不得修改
- `爬虫工具/data/采集日志.txt`、`采集日志_扩展.txt` —— 采集过程的原始输出日志（历史产物）

**口令问题（已核实，不存在问题）：**
已核实不存在明文数据库口令。`项目现状分析.md` §8.3 仅说明口令见本地 `.env`，未记录实际口令；`.env` 已被 `.gitignore` 排除且从未入库。
（原 `README_开发说明.md` 第 107–109 行曾误报"§8.3 写有同一口令且该文件已入库"，经实测：`.env` 中的口令值在 `项目现状分析.md` 全文中命中 0 次，该告警已修正。）

### 待修的文档一致性问题（下一轮文档整理时处理，勿现在动）

- **`详细设计说明书.md` §4.7 的依据引用悬空**：该节依据写「数据需求 §3」，但 `详细设计说明书.md` 并无第 3 章「数据需求」（§3 不存在），指向不明。
  处置：属于**设计文档一致性问题，不是代码问题**；下一轮文档整理时把该引用改成实际存在的章节或文档即可。
  **不要为了修一个引用现在就重开整份详细设计。**

### 已知的文档表述不一致（非路径问题，仅提示）

- 「设计一致性检查项数」已核对确认：`docs/设计阶段一致性检查报告.md`（V1.1）实为 **15 项**（速览表 15 行 + 正文 §1–§15，§16 为留档备查不计），`ReadMe.md` 第 33 行已是 15 项、无需修改；原第 62 行残留旧数字 14，已修正为 15。`开发日报/开发日报.md` 中的 14 项属历史记录，按「只追加，不覆盖」保留不动
- `tmp/check_sql2.py` 提示 `数据库设计说明.md` / `ER图.md` / `设计阶段一致性检查报告.md` 中存在 `21 张表`、`19 张表` 等历史表述（多为"原 21 张 → 精简为 17 张"的说明性文字）
- `tmp/check_ghost.py` 报 15 处 `ml_model` / `llm_failure` / `clean_log` 引用待确认，实际多为 `(原 ml_model)` 这类**已删除说明性语境**，属检查脚本的误报口径

---

## 9. 文档地图

| 用途 | 文件 |
|---|---|
| 项目全局规范（AI 工作守则、开发纪律、日报规则） | `ReadMe.md` |
| 技术路线定稿依据 | `项目现状分析.md` |
| 功能范围与业务规则（**需求基准**） | `开题/业务需求文档.md` |
| 架构与组件编号 | `docs/architecture/C4架构视图.md`、`C4系统上下文图.md`、`C4容器图.md`、`C4组件图.md` |
| 模块 / 接口 / DeepSeek 调用设计 | `docs/design/详细设计说明书.md` |
| 表结构与字段口径 | `docs/database/数据库设计说明.md`、`ER图.md`、`建表脚本.sql` |
| 处理流程 | `docs/diagrams/`（7 份） |
| 设计与数据的一致性核对 | `docs/设计阶段一致性检查报告.md` |
| 数据来源与质量 | `数据采集/采集工作报告.md`、`最终数据质量报告.md` |
| 开发过程记录 | `开发日报/开发日报.md`（及两个正式表格版） |
| 当前可运行性说明 | `README_开发说明.md` |

---

## 10. 运行基线（本机实测）

| 项 | 实测值 |
|---|---|
| Python | 3.11.7（虚拟环境 `.venv`） |
| MySQL | 8.0.32 @ `127.0.0.1:3306`，库 `tibet_review`，`utf8mb4` |
| JDK | 1.8.0_371 |
| Spark | `spark-submit` 已在 PATH |
| HADOOP_HOME | `D:\bigdata\hadoop-3.3.0` |
| 启动方式 | `.\.venv\Scripts\python.exe run.py` → <http://127.0.0.1:5000/> |
| 自检命令 | `.\.venv\Scripts\python.exe scripts\check_env.py` |
