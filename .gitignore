# =============================================================================
#  《基于 DeepSeek 的西藏旅游景点智能评价与分析系统》· Git 忽略规则
#  说明：本文件由开发阶段（阶段一）建立。规则分三类：
#        ① 环境与密钥（绝不入库）   ② 本机大体积历史资料（留在磁盘、不入库）
#        ③ 运行产物与缓存
#  重要：被忽略 ≠ 被删除。以下文件全部保留在本地磁盘，只是不提交到仓库。
#        数据冻结件（最终数据集/来源标注/重复清单）仍以本地文件为唯一权威副本，
#        请按 ReadMe.md「七、版本与备份建议」另行备份到网盘/移动硬盘。
# =============================================================================

# ---------------------------------------------------------------------------
# ① 环境、密钥与编辑器
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# ② 本机大体积历史资料（合计约 2.4 GB，全部保留在磁盘）
# ---------------------------------------------------------------------------

# 浏览器 profile（爬虫运行残留，纯本机缓存，无任何项目价值）
爬虫工具/_edge_profile/
爬虫工具/_edge_profile_ext/
爬虫工具/_edge_profile_probe/

# 原始接口数据与阶段快照（约 1 GB 的 api.jsonl，已永久保留在磁盘，无需重采）
爬虫工具/data/raw/
爬虫工具/data/raw_ext/
爬虫工具/data/out/备份/
爬虫工具/data/out/阶段快照/

# 采集工具产出的中间/衍生数据集（单文件 55–113 MB，且与「数据采集/」下的冻结件重复；
# 超过 Git 托管平台单文件 100 MB 上限，故不入库，只保留在磁盘）
爬虫工具/data/out/*.csv

# 最终数据集与两个大体量版本（单文件 21–116 MB，超过 Git 托管平台单文件 100 MB 上限，
# 因此不入库；它们是「数据冻结」成果，以本地文件为准，另行备份）
数据采集/旅游评论数据集_最终版.csv
数据采集/旅游评论数据集_扩充版.csv
数据采集/进藏沿线扩展数据集.csv
数据采集/旅游评论数据集.csv

# ---------------------------------------------------------------------------
# ③ 运行产物（导入/清洗输出、日志、临时产物）
#    说明：data/ 与 logs/ 只保留 .gitkeep 占位文件，目录内实际产物一律不入库。
# ---------------------------------------------------------------------------
logs/*
!logs/.gitkeep
data/*
!data/.gitkeep
output/
*.log

# 文档生成工具的大体积二进制模板与预览件（tmp/ 下的脚本仍入库）
tmp/*.doc
tmp/*.docx
tmp/*.pptx
tmp/*.xlsx
tmp/~$*
