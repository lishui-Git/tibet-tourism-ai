# 项目现状分析报告

> 课题：《基于 DeepSeek 的西藏旅游景点智能评价与分析系统的设计与实现》
> 阶段：**开题前置 —— 项目现状分析**（本文档不写代码、不重新采集数据）
> 分析依据：对 `旅游评论数据集_最终版.csv`（59,033 行）做**全量逐行扫描**，并交叉核对 `景点来源标注.csv`、`内容重复清单.csv`、`最终数据质量报告.md`、`采集工作报告.md`、`采集进度.csv`、`景点元数据.json`、采集工具源码与日志。
> 复核脚本：`分析脚本/analyze_final.py`、`分析脚本/analyze_extra.py`、`分析脚本/analyze_extra2.py`；原始输出：`分析脚本/现状分析_统计输出.txt`
> 生成时间：本阶段分析

---

## 〇、一句话结论

**当前数据集是一个"单平台（携程）、离线、批量、评论级字段完整、景点级严重长尾"的中文旅游评论语料，共 59,033 条 / 837 个景点 / 15 字段。**
它可以扎实支撑：**数据统计分析 → 情感分析 → 方面（维度）挖掘 → 主题发现 → 景点口碑评价与排行 → 客源地与时间分布分析 → DeepSeek 生成"数据有依据"的景点智能评价报告**。
它**不能**支撑：实时/流式分析、个性化推荐、多平台对比、真实客流量预测、评分回归预测（作为主要指标）。

因此系统定位为：**"Python 数据清洗 + Spark 离线分析 + DeepSeek 智能评价与总结 + 可视化 Web 系统"** 的**离线分析型系统**。

> **修订说明（第三次修订 · 现行版本，以指导教师意见为准）**
> 本文档第一版曾建议"引入流式"，第二版曾按开发环境改设计为"离线全量 + 实时增量"双层架构。**在指导老师明确确认技术路线后（原话："这个毕设我想用 Python 进行数据清洗，Spark 做离线分析，然后 DeepSeek 做智能评价和总结，因为数据主要是历史旅游评论，所以不考虑实时分析了" —— 老师回复"嗯"），本文档按老师意见定稿为"不做实时分析"的纯离线架构。**
> 依据优先级：**① 实际最终数据及报告 → ② 当前毕业设计题目与老师要求 → ③ 通用开发经验**。老师意见高于开发环境清单，故 **Flume / Kafka / Spark Streaming 在本课题中不采用**（理由见 §5.3 X1、§7.0）。

---

## 一、数据集实际规模与字段结构（实测）

### 1.1 规模

| 指标 | 实测值 | 说明 |
|---|---|---|
| 总记录数 | **59,033** | 逐行扫描计数，与报告一致 |
| 评论编号去重后 | **59,033** | 重复 ID 行数 = **0**，主键 100% 唯一 |
| 覆盖景点 | **837** | 与 `景点来源标注.csv` 的 837 行完全对应 |
| 独立用户昵称 | **32,532** | 昵称非稳定 ID，含 5,972 条"匿名用户" |
| IP 归属地取值种类 | **66** | 含 34 个省级行政区 + 港澳台 + 20 个境外国家 |
| 时间跨度 | **2015-06-19 ~ 2026-09-16** | 日期格式异常 **0** 条 |
| 文件体积 | 约 118 MB | UTF-8 with BOM |
| 字段数 | **15** | 与原始数据字段顺序、含义完全一致，可直接纵向合并 |

### 1.2 字段结构（15 列，按数据性质分三类）

| # | 字段 | 类型 | 性质 | 实测质量 |
|---|---|---|---|---|
| 1 | 景点名称 | 文本 | 主键维度 | 空值 0 |
| 2 | 评论编号 | 数值 | **记录主键** | 空值 0，唯一率 100% |
| 3 | 评分 | 1–5 整数 | 定量标签 | 空值 37（0.06%） |
| 4 | 评分描述 | 文本 | 评分映射（1不佳/2一般/3不错/4满意/5超棒） | 空值 37，与评分**完全一一对应，无矛盾** |
| 5 | 评论内容 | 长文本 | **核心语义字段** | 空值 1 条 |
| 6 | 发布时间 | 日期 | 时间维度 | 空值 0，格式统一 `YYYY-MM-DD` |
| 7 | IP归属地 | 文本 | 客源维度 | 空值 0，但历史段大面积为"未知"（见 §3.5） |
| 8 | 用户昵称 | 文本 | 用户维度（弱） | 空值 0 |
| 9 | 点赞数 | 整数 | 互动/权重 | 空值 0，非零 18.35%，最大 588 |
| 10 | 图片数 | 整数 | 互动/权重 | 空值 0，非零 48.35%，最大 18 |
| 11 | 图片URL | 文本 | 媒体 | 非空 48.35%（与图片数>0 的记录**完全吻合**，无矛盾） |
| 12 | 地址 | 文本 | **景点级** | 837/837 景点均有 |
| 13 | 开放时间 | 文本 | 景点级 | 546/837 景点有 |
| 14 | 官方电话 | 文本 | 景点级 | 99/837 景点有 |
| 15 | 景点介绍 | 长文本 | 景点级 | 738/837 景点有 |

**结构结论**：
- 前 11 个字段是**评论级**（每行独立、可直接建模）；后 4 个字段是**景点级**（同一景点内完全重复），建模时必须拆成独立的"景点维表"，**不能作为评论特征直接入模**，否则造成信息泄漏与特征冗余。
- 字段中**没有**：用户 ID、设备信息、订单/价格、游玩时长、同行类型、门票消费、地理位置坐标、经纬度。这直接决定了后面 §4、§5 的功能取舍。

---

## 二、数据来源、景点覆盖与评论特征

### 2.1 数据来源

- **唯一来源平台：携程**（`m.ctrip.com` 公开点评接口 + `you.ctrip.com` 景点页）。无第二平台数据。
- **两个来源口径**（`景点来源标注.csv` 实测）：

| 来源口径 | 景点数 | 条数 | 占比 |
|---|---|---|---|
| 西藏（主） | 554 | 31,752 | 53.79% |
| 进藏沿线（川藏/滇藏/青藏线） | 283 | 27,281 | 46.21% |
| 合计 | **837** | **59,033** | 100% |

- 按景点地址所在省区分布：西藏自治区 302 + 拉萨市 62 + 林芝市 39 + 日喀则市 38 + 山南市 13 + 昌都市 10 + 那曲市 8 + 阿里 8 ≈ **480 个景点属西藏**；四川省 99（含甘孜州 59）、云南省 53（含迪庆州 37）、青海省 20（含玉树 6+2）等属进藏沿线。
- ⚠️ **论文表述必须准确**：数据集是**"西藏及进藏沿线"**，不是纯西藏。若答辩时以"西藏数据集"相称，与"普达措国家公园（云南）""稻城亚丁（四川）"这类头部景点会自相矛盾。建议统一表述为"**西藏及进藏沿线（川藏/滇藏/青藏线）旅游景点评论数据集**"，并说明构成。

### 2.2 景点覆盖：极端长尾，是本数据集最重要的结构特征

| 档位 | 景点数 | 合计条数 | 条数占比 |
|---|---|---|---|
| ≥1,000 条 | 18 | 41,410 | **70.1%** |
| 301–999 条 | 9 | 4,344 | 7.4% |
| 101–300 条 | 30 | 4,808 | 8.1% |
| 31–100 条 | 70 | 3,915 | 6.6% |
| 11–30 条 | 142 | 2,558 | 4.3% |
| 1–10 条 | 568 | 1,998 | 3.4% |

- 最多：布达拉宫 3,965 条；**中位数仅 5 条**；最少 1 条。
- **18 个景点吃掉 70% 的数据量**，568 个景点（67.9%）每条目不足 10 条评论。

**按条数门槛的可用样本量**（决定"哪些景点能做深入分析"）：

| 门槛 | 景点数 | 合计条数 | 占比 |
|---|---|---|---|
| ≥30 条 | 127 | 54,477 | 92.3% |
| ≥50 条 | 92 | 53,097 | 89.9% |
| **≥100 条** | **57** | **50,562** | **85.7%** |
| ≥300 条 | 27 | 45,754 | 77.5% |

**结论**：样本在**评论层面**很充足（5.9 万条，任何 NLP 任务都够），但在**景点层面**极不均衡。
- 全景点统计、整体情感/主题分析 → 全局可行；
- **单景点维度分析（方面挖掘、雷达图、优缺点总结）→ 只能对 ≥100 条的 57 个景点可靠成立**（若要求每个方面都有足够样本，建议 ≥300 条的 27 个作为"深度评价"主体，其余景点做"简版统计"）。

### 2.3 时间分布

| 年份 | 2015 | 2016 | 2017 | 2018 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026(至9月) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 条数 | 13 | 278 | 16 | 22 | 3,086 | 8,758 | 7,452 | 6,132 | 7,737 | 5,060 | 12,609 | 7,870 |

- 2015–2018 合计仅 329 条（0.56%），**2017–2018 几乎空白**，这是携程平台自身的历史数据限制，不是清洗丢失。
- 2019 年起才具备月度趋势分析价值；2020 年疫情低谷、2023 年后回升的形态在数据中**可见**。
- ⚠️ **重要方法学限制**：补抓时"最新排序"只覆盖最近约 2.2 年，"推荐排序"偏近期 ⇒ **数据是"采集时点截面"而非"年度全量"**。因此时间维度的结论只能表述为"**平台评论量的时间分布特征**"，**不能**等同于"客流量"。这一点必须写进论文的"数据局限"。

### 2.4 评论内容特征（实测）

| 特征 | 实测值 |
|---|---|
| 评论长度均值 | 57.5 字 |
| 长度分位 | p10=8 / p25=15 / **p50=31** / p75=70 / p90=125 / p99=416 / max=1,829 |
| 空内容 | 1 条 |
| ≤10 字 | 10,228 条（**17.33%**） |
| 11–30 字 | 19,277 条（32.65%） |
| 31–60 字 | 9,496 条（16.09%） |
| 61–100 字 | 11,527 条（19.53%） |
| 101–200 字 | 6,106 条（10.34%） |
| >200 字 | 2,398 条（4.06%） |

- **一半评论不到 31 字**，近 1/5 在 10 字以内（"值得一去""景色不错"这类）。
- ⇒ 这是个**典型的"短文本 + 高噪声"语料**：结论是
  1. 直接做情感二分类/多分类**可行且样本充足**；
  2. 但**细粒度方面级情感分析（ABSA）的召回会受限**——短评论常常并不点明"门票贵"还是"风景好"，不能指望每条评论都抽得出多个方面；
  3. 建模前应明确一条**低信息量过滤器**（如正文 ≤10 字），并在论文中报告过滤前后的样本量（本数据集相关数据见 §6.5）。

### 2.5 互动与用户特征

| 特征 | 实测值 | 对系统的含义 |
|---|---|---|
| 点赞数非零 | 10,830 条（18.35%），均值 0.43，p90=1，max=588 | 可做**评论热度加权**，但绝大多数为 0，**不宜作为主权重** |
| 图片数非零 | 28,540 条（48.35%），均值 2.04，max 18 | 可做"图文丰富度"辅助指标 |
| 只发 1 条评论的用户 | 26,258 / 32,532 = **80.71%** | 用户维度**稀疏** |
| 评论数 >1 的用户 | 6,274（19.29%） | 昵称非 ID，"匿名用户"占 5,972 条 |
| 高产用户 Top | 匿名用户 5,972、enjoyzent 373、格格巴 355… | 单用户高频评论会影响情感统计，需在论文中说明 |

⚠️ **用户昵称不是用户 ID**：无稳定标识，且存在单一"匿名用户"聚合 5,972 条的情况。任何"用户画像""用户行为建模"都不可靠。

---

## 三、清洗、去重与质量检查结果（依据质量报告 + 实测复核）

### 3.1 采集后清洗链路（工具源码已核实）

`爬虫工具/ctrip_tools.py` 中实际存在的清洗/规范化逻辑：

| 环节 | 实现 | 复核 |
|---|---|---|
| HTML 去标签 | `strip_tags` / `strip_html_simple` | 景点介绍、评论内容均过一遍 |
| JS 转义还原 | `unescape_js` | 处理 `\uXXXX`、`\/` |
| 评论正文换行拍平 | `extract_content` 把换行/多空白压成单空格 | 保证 CSV 单行 |
| 时间标准化 | `parse_publish_time` 把 `/Date(毫秒+0800)/` 转 `YYYY-MM-DD` | 实测日期格式异常 0 条 |
| 评分描述映射 | 由评分整数映射 1不佳…5超棒 | 实测 37 条评分空值对应评分描述也空，无错配 |
| IP 空值填充 | 空则填 `"未知"` | 与 2022 前平台行为一致 |
| 图片URL 拼接 | `images[].imageSrcUrl` 以 `，`（全角逗号）连接 | 实测图片URL非空条数 = 图片数>0 条数，**完全一致** |
| 去重 | 以 `评论编号` 为主键差集 | 实测重复 ID = 0 |

### 3.2 去重结果

| 项目 | 数值 |
|---|---|
| 原始采集条数（未去重） | 38,838 |
| 原有数据条数 | 11,522 |
| **最终去重后** | **59,033** |
| 评论 ID 唯一率 | **100.0000%** |
| 两口径子集 ID 交集 | **0**（西藏 31,752 + 沿线 27,281，天然无重叠） |
| 净增 | **+47,511 条**（相对原始 11,522，扩展 4.1 倍） |

### 3.3 正文重复（**关键，必须写进论文**）

- `内容重复清单.csv` 实测：**1,285 组 / 4,239 条**，占总量 **7.18%**；其中**跨景点重复 759 组**。
- 最大重复组：`"景色不错，"` 出现在 **23 个景点 / 81 条**；`"好好好好好"` 24 个景点 / 62 条。
- 1,285 组中有 **700 组内容 ≤10 字**（短评天然撞车）。
- **处理决策（已由报告确认）**：这些是**不同评论 ID、内容恰好相同的真实评论**，**原文全部保留、未做修改**。
- **对系统设计的直接影响**：`内容重复清单.csv` 必须作为**建模期的可切换过滤清单**使用——
  - 做统计类分析（评分分布、客源分布）→ **保留**（它们代表真实评价次数）；
  - 做主题建模/情感训练 → **建议剔除或降权**（否则"景色不错"会主导主题分布，主题词被少数短句污染）。
  这一点在论文里是加分项：**同一份数据在不同任务下的两种合理口径**。

### 3.4 字段缺失（实测复核，与报告一致）

| 字段 | 空值 | 占比 | 性质判断 |
|---|---|---|---|
| 景点名称/评论编号/发布时间/IP归属地/用户昵称/点赞数/图片数/地址 | 0 | 0% | 完整 |
| 评分 / 评分描述 | 37 | 0.06% | 可用（建模时剔除这 37 条） |
| 评论内容 | 1 | 0.00% | 剔除 1 条 |
| 景点介绍 | 99 个景点无（空值 164 条） | 0.28% | 景点级，仅影响介绍展示 |
| 图片URL | 30,493 | 51.65% | **非缺失**，是该评论本身无图 |
| 开放时间 | 24,610 | 41.69% | 景点级，546/837 景点有 |
| 官方电话 | 32,051 | 54.29% | 景点级，**仅 99/837 景点有** |

**⚠️ 字段覆盖差异的成因（采集工作报告已说明）**：进藏沿线的 283 个景点是用"浏览器直接读页面"的快速方式取景点级信息，导致开放时间、官方电话大面积缺失。**这只影响景点级辅助字段，评论级 11 个字段完全不受影响**。
⇒ **系统设计结论**：景点级缺失字段（电话、开放时间、介绍）**只能作为"有则展示"的辅助信息**，绝不能作为分析维度或算法输入。

### 3.5 IP 归属地的结构性缺失（本节是我复核时新发现的重点）

- 全量 59,033 条中 `IP归属地 = "未知"` 共 **24,025 条（40.70%）**。
- **按年份实测**：

| 年份 | 未知占比 |
|---|---|
| 2015–2021 | **100.0%**（每一年都是 100%） |
| 2022 | 63.3% |
| 2023 | 0.1% |
| 2024 | 0.1% |
| 2025 | 1.0% |
| 2026 | 4.9% |

- 即：**携程自 2022 年 8 月左右才展示 IP 属地**（与"第一批复盘"文档中的判断一致）。
- **有效口径**：`发布时间 ≥ 2022-08-01` 共 **35,098 条（59.5%）**，其中未知仅 520 条（1.5%）。
- 有效口径下客源地 Top：**西藏 23.37%、四川 15.30%、云南 14.59%、广东 6.68%、上海 4.94%、北京 3.94%、江苏 3.81%、浙江 3.17%**。
  （"西藏/四川/云南"高占比同时是采集口径造成的——沿线景点本身就在川滇，这是**必须说明的偏差**。）
- **系统设计结论**：客源地分析模块**必须硬编码限定 2022-08 之后**，并在界面明确标注"数据口径：2022 年 8 月后 35,098 条"，否则结论失真。

### 3.6 评分分布与偏斜（**决定"能不能做评分预测"**）

| 评分 | 条数 | 占比 |
|---|---|---|
| 5（超棒） | 42,337 | **71.72%** |
| 4（满意） | 9,405 | 15.93% |
| 3（不错） | 3,453 | 5.85% |
| 2（一般） | 1,077 | 1.82% |
| 1（不佳） | 2,724 | 4.61% |
| 空 | 37 | 0.06% |

- **5 星占 71.7%**，属典型的平台好评偏斜；1–3 星仅 **7,254 条（12.29%）**。
- 低分评论可得性：≥30 条低分评论的景点仅 **26 个**；≥10 条的 **62 个**；**522/837 个景点零低分评论**。
- 景点级评分差异**真实存在**（这是好消息）：57 个 ≥100 条景点中，5 星占比从 **47.2%（木雅圣地景区）到 97.2%（藏域星球天文体验馆）**，均分从 **3.80 到 4.96**，中位 73.4%。说明**口碑在景点之间有可区分度**，评价类分析有东西可做。
- ⚠️ 存在**异常偏斜景点**，需在论文中标注或单独说明：
  - `藏域星球天文体验馆` 1,102 条、5 星 97.2%、均分 4.96（"第一批复盘"已标记为评分异常）；
  - `奇林峡风景区` 1,115 条、5 星 90.7%；
  - 低分侧：`木雅圣地景区` 均分 3.80、1–2 星 17.1%；`纳帕海(依拉草原)` 1–2 星 21.9%；`米堆冰川` 1–2 星 17.8%。

### 3.7 异常检查

- 报告结论：评分越界 / 日期格式 / 图片数不一致 / 评分描述不匹配 **全部为 0**；我的独立复核（59,033 行逐行）结果一致：
  - 日期格式异常 **0**；
  - 评论 ID 重复 **0**；
  - 评分描述与评分错配 **0**；
  - 图片URL 非空与图片数>0 不匹配 **0**。
- ⇒ **数据集在"字段级一致性"上是干净的**，可直接作为系统的数据事实层。

---

## 四、采集过程还原（依据 `采集工作报告.md` + 工具源码 + 日志文件）

### 4.1 完整链路（已核实代码与产物）

```
① 发现景点：Edge（Playwright, channel=msedge）读携程西藏景点列表页
   → 产出 景点清单_携程.csv(388行) / 景点实测清单.csv / 扩展_景点清单.csv
② 取 poiId：浏览器逐景点取真实 poiId + 景点级信息（地址/开放时间/电话/介绍）
   → 写入 data/out/景点元数据.json（920 KB）
③ 采集评论：requests 直连公开接口 getCommentCollapseList
   每页 50 条，多窗口叠加（推荐/最新/星级分区），单线程低频
   → 写入 data/raw/<景点>/api.jsonl（原始，永久保留）
④ 解析：api.jsonl → 15 字段行（parse / parse-ext）
   → data/out/新抓取_去重.csv、扩展_进藏沿线.csv
⑤ 合并去重：以 评论编号 为主键与原始 11,522 条合并
   → data/out/合并_总表.csv（= 最终数据集）
⑥ 质量检查：mode_quality 生成 质量报告.md（每批次）
   → 15 份阶段快照（data/out/阶段快照/）
```

### 4.2 关键工程事实（论文"数据采集"章节可用）

| 事实 | 数值/结论 |
|---|---|
| 采集方式合规性 | Python requests 调公开接口，**单线程、请求间隔 ≥0.8 秒、未绕过任何安全措施**、不采集非公开数据、仅学术用途 |
| 接口分页硬上限 | `offset ≤ 3000`，单窗口最多 **3,050 条**（50×61 页） |
| 单景点天花板 | 布达拉宫接口池 10,387–10,399 条 → 实际可取 3,965 条（约 38%） |
| 平台景点池 | 4 种排序对比无新增；扩展发现 3,103 个 POI 中属西藏的已采完 |
| 剩余未采景点 | 清单 1,761 个，已采到 555 个，未采 1,217 个，其中 **1,016 个为零点评 POI** |
| 撞顶景点 | 3 个（布达拉宫/纳木措/雅鲁藏布大峡谷）受 3,050 窗口限制 |
| 断点续跑 | 已采评论编号自动跳过，中断可续 |
| 可复现性 | `data/raw/*/api.jsonl` 为原始接口数据，**解析规则变更无需重采** |

### 4.3 版本演进（数据可追溯，答辩时的"过程性"证据）

| 文件 | 条数 | 角色 |
|---|---|---|
| `旅游评论数据集.csv` | 11,522 | **你的原始数据，一字未改**（保留） |
| `旅游评论数据集_扩充版.csv` | 31,752 / 554 景点 | 西藏-only 口径（论文若要严格西藏口径用这份） |
| `进藏沿线扩展数据集.csv` | 27,281 / 283 景点 | 进藏沿线口径 |
| **`旅游评论数据集_最终版.csv`** | **59,033 / 837 景点** | **交付数据集** |
| `爬虫工具/data/out/阶段快照/` | 15 份 | 每批次时间戳快照 + 对应质量报告 |

⇒ **原始→扩充→扩展→合并的每一层都留有产物与快照**，这在毕业设计的"数据工作可信度"上是明显优势，建议在论文数据章节用一张版本演进表呈现。

---

## 五、能力判定：当前数据**能**支撑什么

标注说明：✅ 可靠支撑 / ⚠️ 有条件支撑（须限定范围或口径）/ ❌ 不可支撑

### 5.1 ✅ 完全可靠支撑

| 编号 | 能力 | 数据依据 | 说明 |
|---|---|---|---|
| C1 | **景点基础统计与可视化** | 59,033 条 / 837 景点 / 15 字段全量 | 景点评论量排行、评分分布、时间分布、图文/点赞分布；全量可用 |
| C2 | **评论情感分析（三分类/五分类）** | 59,033 条正文 + 1–5 星标签 | 星级可作**弱监督标签**（1–2 负、3 中、4–5 正）；亦可让 DeepSeek 直接判情感后与星级对比 |
| C3 | **DeepSeek 语义理解与结构化抽取** | 长文本 5.9 万条 | 单条评论 → {情感极性, 情感强度, 涉及方面, 关键句, 摘要}；p50=31 字也足以判极性 |
| C4 | **评论主题发现（无监督）** | 5.9 万条正文 | LDA / BERTopic / 关键词共现；可得到"高原反应/门票价格/交通路况/住宿餐饮/导游服务/景色震撼"等主题 |
| C5 | **景点口碑评价指标计算** | 景点级聚合 | 均分、好评率、低分占比、点赞加权、图文率、评论量热度 → 综合口碑指数；57 个 ≥100 条的景点差异真实（均分 3.80–4.96） |
| C6 | **景点排行与对比** | 同上 | 按口碑/热度/好评率多维排行；并对 ≥100 条的 57 个景点做雷达对比 |
| C7 | **时间趋势分析** | 2019–2026 月度 | 月度评论量、评分年度变化、情感均值年度变化（须声明为"平台评论量"而非客流量） |
| C8 | **客源地分布分析** | 2022-08 后 35,098 条 | 省级/境内外分布、Top 客源省、客源地×景点交叉。**必须限定有效口径** |
| C9 | **景点级辅助信息展示** | 地址 837/837、介绍 738/837 | 景点详情页、地图定位（有文本地址）、介绍摘要 |
| C10 | **数据质量与可复现性说明** | 快照 + 原始 jsonl | 论文数据章节、系统"数据说明"页 |

### 5.2 ⚠️ 有条件支撑（必须限定范围，否则结论不可靠）

| 编号 | 能力 | 限定条件（这是设计红线） |
|---|---|---|
| C11 | **方面级情感分析（ABSA）** | 只能对 **≥100 条的 57 个景点**（覆盖 85.7% 评论）做"景点×方面"结论；且必须报告每个方面的**样本数**，样本 <10 的方面不输出结论。短评论占比高，单条可抽出的方面数有限，**不能承诺"每条评论都抽出多维意见"** |
| C12 | **低频景点的口碑结论** | 568 个景点 <10 条（中位 5 条）→ **只输出"评论量 + 均分"，不给评价性结论**，界面明确标注"样本不足" |
| C13 | **细分群体差异分析** | 只能按"客源地省份 × 景点"或"年份 × 评分"这类**粗粒度**交叉；**不能做用户画像/性别/年龄/同行类型**（字段不存在） |
| C14 | **图片相关分析** | 只有"图片数"和图片 URL，**无图像内容**。可做图文率统计；若要用图，需另做图像模型（超范围，不建议） |
| C15 | **节假日/季节效应** | 只能用发布日期近似，且"发布日期 ≠ 游玩日期"；只能表述为"发布时点分布" |

### 5.3 ❌ 不可支撑（**明确不设计，避免"堆技术"**）

| 编号 | 能力 | 为什么不做（数据 + 老师意见层面的理由） |
|---|---|---|
| **X1** | **实时/流式分析（Flume / Kafka / Spark Streaming / Flink / Structured Streaming）** | ① **老师已明确"不考虑实时分析"**；② 数据是**一次性离线采集的静态 CSV**，无实时数据源、无上游生产者，引入消息队列只会退化成"把 CSV 灌进 Kafka 再读出来"的表演性架构；③ 系统定位是**历史评论的离线分析**，实时链路不产生任何业务价值。**本条为现行定稿决策。** |
| X2 | **个性化推荐系统（协同过滤）** | **无用户 ID、无行为序列、无浏览/购买/收藏日志**。用户标识只有昵称且 80.7% 只评论 1 条、另有 5,972 条统一叫"匿名用户"，**用户-物品矩阵根本无法构建**（可降级方案见 §5.4） |
| X3 | **多平台对比分析** | 只有携程一家；其他平台字段结构不同（无"评分描述"），会破坏字段统一性 |
| X4 | **真实客流量/客流预测** | 评论量 ≠ 客流量，且时间序列因采集窗口偏斜而**不完整**，做预测会得到"预测评论条数"这种无业务意义的结论 |
| X5 | **评分回归预测（作为核心实验）** | 评分 71.7% 为 5 星、方差极小，回归器极易退化为"恒输出 4.9"，RMSE 差异被多数类吞掉。**可以做，但只能作为辅助对照**（且必须报告类别分布作为解释） |
| X6 | **虚假评论/水军检测** | 无标注真值、无用户行为特征（IP 精确度只到省、无设备/频率），只能做启发式猜测，**结论不可验证** |
| X7 | **商家/景区运营决策建议（价格、库存）** | 无价格、销量、库存、成本字段；模型只能输出文本层面的意见归纳，不能给经营量化决策 |
| X8 | **跨景点"同质化"深度比对 / 地理空间分析** | 只有文本地址，**无经纬度**；做地图只能做点位聚合展示，做不了空间自相关、可达性分析 |
| X9 | **多模态（图像情感）分析** | 只有图片 URL 且 51.65% 无图，图片未下载、无标注，投入产出比极低 |


### 5.4 关于推荐功能的一个折中说明（重要）

"西藏旅游景点智能评价与分析系统"里出现"推荐"字样很自然，但**必须换一个数据上站得住的形式**：

- ❌ 不做：`用户 — 行为 — 物品` 的个性化推荐（数据不存在）。
- ⚠️ 可做（若你希望保留推荐模块）：**基于内容标签的相似景点检索 / 冷启动推荐**——用 DeepSeek 从评论中抽取的**景点特征标签 + 方面情感画像**（如"湖景/雪山/寺庙/高反明显/门票偏贵/适合摄影"）构建景点画像向量，做"**找了布达拉宫的人，也可以看这些相似景点**"或"按偏好筛选景点"。
  这在方法上属于**基于内容的推荐（Content-Based）**，数据上是自洽的，但必须在论文中**明确定位为"基于景点内容画像的相似推荐"，不声称个性化**。
- 建议优先级：**低于**情感分析、方面挖掘、智能评价生成（这三个是本课题"DeepSeek"的落点）。若时间紧张，可作为可选扩展模块。

---

## 六、系统功能模块设计建议（依据实际数据能力反推）

设计原则：**每一项功能都能指出它用的是哪些字段、覆盖多少条数据、样本不足时怎么降级。**

### 6.1 模块总览

| 模块 | 名称 | 数据依赖 | 覆盖范围 |
|---|---|---|---|
| **M0** | 数据层与数据说明 | 最终数据集 + 质量报告 + 快照 | 全量 |
| **M1** | 数据总览与统计分析 | 全部 15 字段 | 全量 59,033 条 / 837 景点 |
| **M2** | DeepSeek 评论智能理解 | 评论内容 + 评分 + 发布时间 | 全量（可抽样 + 批处理落库） |
| **M3** | 情感分析（含基线对比） | 评论内容 + 评分（弱标签） | 全量 |
| **M4** | 主题/方面挖掘 | 评论内容 | 全量做主题；方面按景点限定 ≥100 条 |
| **M5** | 景点口碑评价与智能评价报告 | M2/M3/M4 结构化结果 + 景点级字段 | 57 个 ≥100 条景点出完整报告；其余出简版 |
| **M6** | 景点排行与多维对比 | 聚合指标 | 全量排行；雷达对比限 57 个 |
| **M7** | 时空分布分析 | 发布时间 + IP归属地 | 趋势 2019+；客源地限 2022-08 后 |
| **M8** | 相似景点推荐（可选） | M4 景点画像 + 标签 | 有标签的景点 |
| **M9** | 系统管理（登录/权限/数据管理/任务管理） | — | 常规 |
| **M10** | **Spark 离线分析引擎（后台模块）** | `review` 清洗后全量数据 | 全量 59,033 条（本课题"Spark 做离线分析"的落点，见 §7.0） |

### 6.2 各模块要点

**M1 数据总览与统计分析**
评论量与评分分布、景点 Top 排行、时间分布、评论长度分布、图文/点赞分布、来源口径构成（西藏 / 进藏沿线）、字段缺失情况。全部为**确定性事实**，无需模型，是系统的"可信底座"。

**M2 DeepSeek 评论智能理解（本课题的技术核心）**
输入单条评论 → 输出结构化 JSON：
```json
{
  "sentiment": "正面/中性/负面",
  "intensity": 1-5,
  "aspects": [{"name":"风景","polarity":"正面","evidence":"..."},
              {"name":"门票","polarity":"负面","evidence":"..."}],
  "keywords": ["雪山","高反","区间车"],
  "summary": "一句话摘要"
}
```
工程要点（本科可行、成本可控）：
- **批处理 + 落库**：离线跑一遍全量（或分层抽样：全量短评规则处理 + 长评 DeepSeek 处理），结果写入 MySQL，线上页面**读库不调接口**（避免每次刷新都调 API、避免超时）；
- **JSON 强约束 + 校验重试**：要求返回 JSON，解析失败自动重试；
- **few-shot 提示词**：给 3–5 条西藏旅游领域示例，提升稳定性；
- **必须做一致性评估**：随机抽 200–300 条人工标注，报告 DeepSeek 与人工的一致率（Kappa），这是论文的实验证据。

**M3 情感分析（含基线对比）**
- 主方法：DeepSeek 零样本/few-shot 情感判定；
- 对比基线（至少两个，构成"实验对比"章节）：
  1. **传统词典法**（如 SnowNLP / 知网 HowNet 情感词典 + 旅游领域词）；
  2. **机器学习法**（TF-IDF + 朴素贝叶斯/逻辑回归/SVM），以星级为弱标签；
  - 可选：中文预训练模型微调（BERT/RoBERTa-wwm），若有算力。
- 评测：准确率、F1、混淆矩阵、Kappa；并**必须报告类别不平衡**（5 星 71.7%）带来的影响与缓解措施（类别权重/重采样/分层评估）。

**M4 主题/方面挖掘**
- 全量主题：LDA 或 BERTopic（或 jieba 分词 + 关键词共现 + 词云）→ 得到数据集级主题图谱与主题×年份演化；
- 景点方面：DeepSeek 方面抽取 → 聚合为"景点 × 方面 × 情感占比"矩阵；
- **诚实呈现**：每个方面都显示样本数，样本 <10 不展示结论；
- 低信息量评论处理：`正文 ≤10 字` 单独统计（17.33%），建模时按策略过滤并报告过滤前后差异；`内容重复清单.csv` 作为可切换开关。

**M5 景点口碑评价与智能评价报告（DeepSeek 的落点）**
关键设计：**禁止让大模型"凭空点评"**。采用 **RAG 式"数据事实约束生成"**：
1. 系统先算出该景点的**事实包**：评论总量、均分、好评/中评/差评占比、各月趋势、各来源省占比、Top 方面及各自情感占比、最具代表性的正/负面原文（按点赞与长度选取，去重）；
2. 把事实包作为上下文交给 DeepSeek，要求**只能依据给定事实**生成：优点归纳、不足归纳、适合人群、游玩提示、综合口碑结论；
3. 输出中标注引用的事实来源（如"依据 483 条 1–3 星评论中'门票与区间车'类反馈"）。
这样得到的评价**可追溯、可复现、不会胡说**，也正是本课题"基于 DeepSeek 的智能评价"的学术卖点。
同时**硬性降级规则**：评论 <10 条的 568 个景点 → 只显示统计信息 + "样本不足，暂不生成评价"。

**M6 排行与对比**
- 排行：口碑综合分（均分 + 好评率 + 点赞加权 + 评论量热度，权重可配置）、热度榜、好评率榜、低分关注榜；
- 对比：对 57 个 ≥100 条景点做雷达图（风景/服务/交通/价格/体验等维度，来源于 M4 方面情感），**必须显示每个轴的样本量**。

**M7 时空分布**
- 时间：月度/年度评论量与情感均值（限 2019 年之后，界面注明"采集截面，非客流量"）；
- 空间：客源地省市分布（**硬编码 2022-08 之后口径**，界面注明"有效样本 35,098 条"）、客源地×景点交叉；
- 景点分布：按来源口径/所在省区聚合（西藏 554 / 沿线 283）。

**M8 相似景点推荐（可选，定位为基于内容的相似检索）**
用 M4 的景点特征标签 + 方面画像构建向量，输出"相似景点"，并显示推荐理由（共有标签）。不声称个性化。

**M9 系统管理**
登录注册、角色权限（管理员/普通用户）、数据集版本管理（关联阶段快照）、**离线分析任务管理**（触发 Spark 作业与 DeepSeek 批处理、查看运行进度与日志、断点续跑）、分析结果版本与重算记录。**这是本科毕设的常规要求项，保留但不做重点。**

**M10 Spark 离线分析引擎（后台模块，非界面功能）**
本模块不对应前端页面，而是系统的计算核心，对应老师要求的"**Spark 做离线分析**"：
- **Spark SQL 多维统计**：景点/年份/季度/客源地/来源口径的交叉聚合指标，写入 `stat_*` 表；
- **Spark MLlib 情感模型**：以星级为弱标签训练 TF-IDF + 朴素贝叶斯/逻辑回归三分类模型，持久化到 `ml_model`，供全量批量推理，并与 DeepSeek 结果对比；
- **Spark MLlib LDA 主题发现**：全量评论主题与主题词分布，写入 `topic` / `topic_word`；
- **与 Python 清洗层的边界**：Python 负责**行级清洗与文本预处理**（灵活易调），Spark 负责**全量聚合与模型训练**（体现分布式计算能力），两者职责不重叠（§7.1 架构纪律 1）。

---

## 七、技术路线建议（定稿：Python 清洗 + Spark 离线分析 + DeepSeek 智能评价与总结）

### 7.0 技术路线定稿依据

**指导老师已确认技术路线**（原文："这个毕设我想用 **Python 进行数据清洗**，**Spark 做离线分析**，然后 **DeepSeek 做智能评价和总结**，因为数据主要是历史旅游评论，所以**不考虑实时分析**了" —— 老师回复"嗯"）。

因此本课题**定稿为纯离线分析架构**，三层职责如下：

| 层 | 职责 | 技术 | 产出 |
|---|---|---|---|
| **数据清洗层** | 把 15 列原始 CSV 变成可分析、可入库的规范数据 | **Python**（pandas + jieba + 正则） | `review`（评论明细，59,033 条）/ `spot`（景点维表，837 条）/ 清洗日志 |
| **离线分析层** | 全量多维统计、情感模型训练、主题发现 | **Spark 3.3.1**（Spark SQL + MLlib，Scala 2.13.10 / PySpark） | 全量指标表、MLlib 情感模型、LDA 主题结果 |
| **智能评价层** | 情感与方面抽取、景点智能评价与总结 | **DeepSeek API** + 事实包约束 | `sentiment` / `aspect` / `spot_report` |
| **展示层** | 可视化与交互 | Flask + ECharts + MySQL8 | Web 系统 |

**明确不采用（老师已定）**：`Flume / Kafka / Spark Streaming / Structured Streaming / Flink / 实时大屏`。
理由（论文"技术选型"章节可直接引用）：**本课题语料为历史旅游评论的静态快照，不存在实时数据源；系统的分析对象是"已沉淀的全量评论"，而非"持续到达的数据流"。引入消息队列与流式计算不产生业务价值，只会增加部署复杂度与答辩风险。**

> **关于你已具备的 Flume / Kafka / OffsetExplorer 环境**：本课题不使用，但这不影响你已装好的环境（它们只是闲置）。**不要为了"用上它们"而硬塞实时链路**——这正是老师否掉的方向，也是你原始要求里明确反对的"堆技术"。若后续老师要求补一个"可扩展性"亮点，可在论文"未来工作"中用一段话说明"若接入增量采集，可通过 Kafka + Spark Streaming 扩展为增量分析"，**仅作为展望，不实现**。
### 7.1 总体架构（四层离线架构）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ ① 数据源层（上一阶段已完成，本阶段不重采、不重爬）                              │
│   旅游评论数据集_最终版.csv (59,033 × 15)                                  │
│   + 景点来源标注.csv / 内容重复清单.csv / 采集失败景点清单.csv                  │
│   + 爬虫工具/data/out/阶段快照 (15 份，可追溯)                                │
│   说明：数据为历史评论静态快照，一次性采集完成，无增量数据源                        │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │ 读取
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ② 数据清洗层（Python：pandas + jieba + 正则）                                │
│   · 字段规范化：日期→年/月/季；IP归属地→省级行政区标准化；评分→数值+极性标签      │
│   · 评论级 / 景点级字段拆分（后 4 字段拆为景点维表，避免特征泄漏）                 │
│   · 低信息量标记（正文 ≤10 字，17.33%）+ 内容重复清单作为可切换过滤项             │
│   · 清洗日志与统计（每步处理量、丢弃量、异常量，可复核）                           │
│   → 输出规范 CSV + 写入 MySQL: review / spot / clean_log                   │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ③ 离线分析层（Spark 3.3.1：Spark SQL + MLlib，Scala 2.13.10 / JDK1.8）      │
│   ├─ Spark SQL 多维统计：景点/年份/季度/客源地 → 全量指标表                    │
│   ├─ MLlib 情感模型：TF-IDF + 朴素贝叶斯/逻辑回归，以星级为弱标签训练            │
│   │    → 模型持久化（供离线批量推理，并与 DeepSeek 结果做对比实验）              │
│   ├─ MLlib LDA 主题发现：全量评论主题及主题词分布                              │
│   └─ 结果写入 MySQL：stat_spot / stat_time / stat_ip / topic / ml_sentiment │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ④ 智能评价层（DeepSeek API 批处理，本课题的核心亮点）                          │
│   ├─ 情感 + 方面 + 关键词 + 一句话摘要（JSON 结构化落库）                       │
│   ├─ 事实包构造：景点统计事实 + 代表性正/负面原文（按点赞与长度选取去重）           │
│   ├─ 景点智能评价与总结生成（事实约束 + 引用依据，禁止凭空生成）                   │
│   └─ 与 MLlib / 词典法 的一致性对比评估（论文实验章节数据）                      │
│   → 写入 MySQL: sentiment / aspect / spot_report / eval_log                │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ⑤ 存储层：MySQL 8 @ localhost:3306（库名建议 tibet_review；口令见本地配置文件，不写入文档）    │
│   review / spot / clean_log / stat_spot / stat_time / stat_ip /            │
│   sentiment / aspect / topic / spot_report / ml_model / user / task_log    │
│   （字符集 utf8mb4；连接串须加 allowPublicKeyRetrieval=true）                │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ⑥ 应用层：Flask（REST API，只读 MySQL，请求中不调用大模型）                     │
│   数据总览 / 景点列表与详情 / 情感分析 / 主题分析 / 排行对比 / 时空分布 /          │
│   景点智能评价报告 / 相似景点(可选) / 数据说明 / 登录权限                        │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│ ⑦ 可视化层：ECharts（柱 / 折线 / 饼 / 热力 / 词云 / 雷达 / 散点）                │
└──────────────────────────────────────────────────────────────────────────┘
```

**架构纪律（答辩要点）**：
1. **Python 与 Spark 分工明确、不重叠**：Python 做**行级清洗与文本预处理**（灵活、易调试），Spark 做**全量聚合与模型训练**（体现分布式计算能力）。同一件事不用两套技术做（避免"为了用 Spark 而用 Spark"的质疑）。
2. **数据只在离线流动一次**：CSV → 清洗 → Spark → DeepSeek → MySQL，全链路**可重跑、可断点、可追溯**（每步写 `task_log`）。
3. **Web 层只读库**：页面刷新不触发大模型调用与 Spark 作业，保证演示稳定与响应速度。

### 7.2 数据表清单（初稿，详细设计文档给完整 DDL）

| 类别 | 表名 | 用途 | 关键约束 |
|---|---|---|---|
| 清洗 | `review` | 评论明细 59,033 条（含规范化字段、情感/方面外键） | PK `comment_id` |
| 清洗 | `spot` | 景点维表 837 条（来源口径、地址、介绍、开放时间、电话） | PK `spot_name` |
| 清洗 | `clean_log` | 清洗步骤日志（每步处理量/丢弃量/异常量） | 自增 |
| 统计 | `stat_spot` | 景点级全量指标（评论数、均分、好评率、差评率、图文率…） | PK `spot_name` |
| 统计 | `stat_time` | 时间维度指标（年/月/季度评论量与情感均值） | PK `(period_type, period)` |
| 统计 | `stat_ip` | 客源地指标（**限定 2022-08 后口径**） | PK `ip_province` |
| 智能 | `sentiment` | 三路情感结果并列（DeepSeek / MLlib / 词典法），供对比实验 | PK `(comment_id, method)` |
| 智能 | `aspect` | 方面级结果（方面名、极性、证据句） | PK `(comment_id, aspect_name)` |
| 智能 | `topic` / `topic_word` | 主题与主题词分布 | PK `topic_id` |
| 智能 | `spot_report` | 景点智能评价报告（事实包快照 + 生成文本 + 引用依据） | PK `spot_name` |
| 运维 | `ml_model` | MLlib 模型持久化记录（版本、路径、训练指标） | PK `model_version` |
| 运维 | `user` | 系统用户与角色 | PK `user_id` |
| 运维 | `task_log` | 离线任务运行日志（可追溯、可断点） | 自增 |

### 7.3 技术选型表（定稿）

| 层 | 选型 | 版本 / 说明 |
|---|---|---|
| 数据清洗 | **Python**（pandas + jieba + re） | 本机 Python 3.11.7；**注意**：PySpark 3.3.1 官方验证到 Python 3.10，若 PySpark 在 3.11 下报错，用 conda/venv 建 **Python 3.10** 环境（§8.4 K11） |
| 离线分析 | **Spark 3.3.1**（Spark SQL + MLlib） | 运行在 **JDK 1.8**；Scala **2.13.10**（`_2.13` 构件） |
| 离线分析（语言选择） | **Scala 写 Spark 作业（主）+ PySpark 可选** | 你有 Scala 2.13.10 + IDEA + Maven 环境，Scala 版更贴合"大数据开发"的答辩印象；DeepSeek 批处理仍用 Python（调用 API 更方便）。**两者通过 MySQL 解耦，互不依赖** |
| 大模型 | **DeepSeek API** | 离线批处理 + 结果落库；建议 `openai` SDK（DeepSeek 兼容 OpenAI 协议）或 `requests` |
| 传统模型 | Spark **MLlib**（TF-IDF + NB/LR）+ scikit-learn / SnowNLP（词典基线） | MLlib 用于离线批量推理；sklearn/SnowNLP 用于对比实验 |
| 主题模型 | Spark **MLlib LDA** | 与离线层技术栈统一；备选 gensim |
| 存储 | **MySQL 8** @ `localhost:3306` | **口令见本地 `.env`（不写入文档、不提交仓库）**；库名 `tibet_review`；客户端 Navicat |
| 后端 | **Flask + PyMySQL / SQLAlchemy** | 只读 MySQL 做 REST API |
| 前端 | **HTML + ECharts**（Jinja2 渲染 + axios 局部刷新） | **不引入 Vue/Node 构建链**，降低演示与部署风险（若你已熟悉 Vue，可选 Vue3，但非必需） |
| 构建 | **Maven 父子工程** | 与你 IDEA2024 + Maven 习惯一致 |
| 运行环境 | JDK 1.8、Windows 本机、`local[*]` | 需配置 `winutils.exe` / `HADOOP_HOME`（§8.4） |
| 文档/图表 | Markdown + Mermaid | 业务需求 / C4 视图 / 详细设计 / ER / 流程图 |

**明确不引入**：`Flume / Kafka / OffsetExplorer / Spark Streaming / Structured Streaming / Flink / Hive / HBase / Hadoop 集群 / Redis / HDFS / Docker / K8s / 微服务 / 个性化推荐（协同过滤）`。
统一理由（论文技术选型章节可直接引用）：**本课题数据为单平台历史评论的静态快照（5.9 万条，单机可处理），无实时数据源、无高并发需求；分析任务是一次性批处理而非持续流计算。因此不引入消息队列、流式计算与分布式存储组件，避免无收益的复杂度与部署风险。**

### 7.4 DeepSeek 的落点与批量调用工程要点

| 落点 | 任务 | 说明 |
|---|---|---|
| L1 | 评论情感极性 + 强度 | 全量批处理，与 MLlib/词典法结果并列存储用于对比实验 |
| L2 | 方面/维度抽取 | "区间车 / 氧气瓶 / 观景台 / 门票 / 导游"等领域表达 |
| L3 | 关键词与一句话摘要 | 生成式任务 |
| L4 | **景点智能评价与总结生成** | 事实包约束 + 引用依据，是本课题"智能评价"的直接体现 |
| L5 | 景点标签（供相似推荐，可选） | 标签抽取 |
| L6 | 与词典法/MLlib 的一致性对比实验 | 论文实验章节 |

**批量调用工程要点（本科可行、成本可控、可复现）**：
1. **分层处理策略**：正文 ≤10 字的 10,228 条（17.33%）用规则/词典快速判定，**不调用大模型**；长评论优先、按景点分层抽样，控制调用量；
2. **结果落库 + 断点续跑**：每条处理结果立即写 `sentiment`/`aspect`，已处理的 `comment_id` 自动跳过；
3. **文本缓存去重**：1,285 组重复正文（4,239 条）只调用一次，结果复用；
4. **JSON 强约束 + 校验重试**：要求返回 JSON，解析失败自动重试（最多 2 次），仍失败记入失败队列；
5. **限并发 + 限速**：建议并发 3–5，避免限流；失败退避重试；
6. **一致性评估（论文必需）**：随机抽 200–300 条人工标注，报告 DeepSeek 与人工的一致率 / Kappa，作为实验证据。

> **论文核心实验命题**：*"在西藏旅游短评论这一领域、标注稀缺（以星级为弱标签）的条件下，DeepSeek 零样本/few-shot 情感与方面抽取，相比传统词典法与 Spark MLlib 浅层模型的效果差异及其适用边界。"* —— 该命题完全由现有数据支撑，工作量适合本科。

### 7.5 Maven 父子工程结构（建议）

```
tibet-review-analysis/                  (父 POM：JDK1.8 / Scala2.13.10 / Spark3.3.1)
├─ pom.xml
├─ tibet-offline/                       离线分析（Scala + Spark SQL + MLlib）
│   └─ 清洗后数据读取 / 多维统计 / 情感模型训练与推理 / LDA 主题 / 指标落库
├─ db/                                  MySQL 建表与初始化 SQL（由详细设计文档生成）
│   └─ 01_schema.sql / 02_init_spot.sql / 03_index.sql
├─ clean/                               数据清洗（Python + pandas）
│   └─ 字段规范化 / 评论级与景点级拆分 / 低信息量与重复标记 / 清洗日志
├─ llm-batch/                           DeepSeek 批处理（Python）
│   └─ 分层策略 / 事实包构造 / 限并发与缓存 / 断点续跑 / 评价报告生成 / 一致性评估
├─ web/                                 Flask + ECharts 应用
│   ├─ app.py / api/ / templates/ / static/
└─ tools/                               辅助脚本
    ├─ init_check.py                    环境自检（Python 版本 / MySQL 3306 / 密码 / 表结构 / JDK与Spark）
    └─ run_all.ps1                      一键串行执行：清洗 → Spark 分析 → DeepSeek 批处理 → 启动 Web
```

> `run_all.ps1` + `init_check.py` 直接对应你《Trae提示词(4).txt》里"**创建一个一键启动/关闭脚本**"与"**检查环境是否就绪**"的习惯，本项目保留这两项。

### 7.6 分阶段推进路线

| 阶段 | 产出物 | 说明 |
|---|---|---|
| S1（当前） | **项目现状分析报告**（本文档） | 现状盘点 / 数据可行性论证 / 技术路线定稿 |
| S2 | 业务需求文档 | 功能需求、非功能需求、用例图、**数据口径约束**（2022-08、≥100 条门槛、样本不足降级） |
| S3 | 系统设计文档 | 架构图、C4 上下文/容器/组件视图、模块划分、Mermaid 流程图（数据流图 + 离线处理时序图） |
| S4 | 数据库设计 | ER 图 + 建表 SQL（清洗表 + 统计表 + 智能结果表 + 运维表） |
| S5 | 数据清洗实现（Python） | 字段规范化、维度拆分、低信息量与重复标记、清洗日志与统计 |
| S6 | 离线分析实现（Spark/Scala） | Spark SQL 多维统计 + MLlib 情感模型训练与批量推理 + LDA 主题 + 指标落库 |
| S7 | DeepSeek 智能评价实现（Python） | 分层批处理 + 事实包构造 + 评价报告生成 + 一致性评估实验 |
| S8 | Web 系统实现 | Flask API + ECharts 各分析页面 + 登录权限 + 数据说明页 |
| S9 | 测试与验收 | 功能核对清单 + **与业务需求文档逐条比对** + 全链路重跑验证（清洗→分析→评价结果可复现） |

> 从《Trae提示词(4).txt》继承的**通用**开发经验（本文档采纳）：文档先行（业务需求→架构→详细设计→SQL→代码）、**每步"对照文档检查是否有遗漏"**、代码加注释、输出中文化、给出完整可运行成果、分析点拆成可独立运行的子模块、一键启停脚本、环境自检。**其中电商业务、实时分析、实时推荐、Vue 前端构建链等具体方案不适用于本课题；Kafka/Flume/Spark Streaming 已按老师意见明确不采用。**


---

## 八、技术环境适配与关键坑位（JDK1.8 专用，动手前必读）

### 8.1 版本兼容性结论

| 组合 | 结论 | 依据 / 说明 |
|---|---|---|
| **JDK 1.8 + Spark 3.3.1** | **✅ 官方支持**（[Spark 3.3.1 官方文档](https://ftp.riken.jp/net/apache/spark/docs/3.3.1/)原文：*"Spark runs on Java 8/11/17, Scala 2.12/2.13"*） | 3.3.x 是**最后一个正式支持 Java 8 的 Spark 版本线**（3.5 起要求 Java 17）。**你选 Spark 3.3.1 是正确的，不要升级**，否则必须换 JDK。另注：Java 8u201 之前的版本自 Spark 3.2.0 起被标记为 deprecated |
| **Scala 2.13.10 + Spark 3.3.1** | ✅ 可用（须用 `_2.13` 构件） | 官方文档明确要求："*when using Scala 2.13, use Spark compiled for 2.13, and compile code/applications for Scala 2.13 as well*"。Spark 3.3.1 的 2.13 构建内部编译版本为 2.13.8，2.13.x 系列**二进制兼容**，IDE 中 `scala.version` 设 2.13.10 即可 |
| **Kafka / Flume / OffsetExplorer** | — | **本课题不采用**（老师已定：不做实时分析）。你已装好的环境保持闲置即可，不参与本系统运行，也不需要卸载 |

### 8.2 必须锁定的依赖坐标（Scala 2.13 后缀，写错就跑不起来）

```
spark-core_2.13          3.3.1
spark-sql_2.13           3.3.1
spark-mllib_2.13         3.3.1      ← 情感模型训练 / LDA 主题
mysql-connector-j        8.0.33     （或 8.0.28+，MySQL8 必须 8.x 驱动）
```

> ⚠️ 常见踩坑：网上教程多用 `_2.12`。你环境是 Scala 2.13.10，**所有 Spark 构件必须 `_2.13`**，否则运行期抛 `NoSuchMethodError` / `NoClassDefFoundError`。
> 说明：因不采用流式，**不需要** `spark-streaming_2.13`、`spark-streaming-kafka-0-10_2.13`、`spark-token-provider-kafka-0-10_2.13` 等构件。

### 8.3 本机运行参数

| 项 | 值 |
|---|---|
| MySQL | `jdbc:mysql://localhost:3306/tibet_review?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true` |
| MySQL 账号 | `root` / **口令见本地 `.env`（不写入文档、不提交仓库）** |
| Spark 运行模式 | `local[*]`（开发）；提交时 `--master local[*]` |
| Spark 作业参数 | JDK1.8 下 `-Xms512m -Xmx2g`（5.9 万条规模，单机内存充裕） |
| 存储引擎 | MySQL 8 + InnoDB，字符集 `utf8mb4` |
| Kafka | **不使用** |

### 8.4 Windows 本地运行必踩的坑（提前规避）

| # | 坑 | 症状 | 处理 |
|---|---|---|---|
| K1 | **缺 winutils.exe / HADOOP_HOME** | 启动即 `java.io.IOException: Could not locate executable null\bin\winutils.exe` | 放置 `winutils.exe` 到 `D:\hadoop\bin`，设 `HADOOP_HOME=D:\hadoop` 并加入 `PATH` |
| K2 | JDK17 风格的 JVM 参数照抄 | `Unrecognized VM option 'UseG1GC'` 或模块化报错 | JDK1.8 下用 `-Xms512m -Xmx2g` |
| K3 | Scala case class / 闭包序列化 | `Task not serializable` | 所有进入 Spark 闭包的对象（含 MySQL 连接配置类）必须 `extends Serializable`；**JDBC 连接不要跨闭包传递**，在 `foreachPartition` 内部创建 |
| K4 | MySQL 驱动重复/缺失 | `No suitable driver found` | 驱动随 `--jars` 或打进 fat jar |
| K5 | MySQL8 认证插件 | `Public Key Retrieval is not allowed` | 连接串必须加 `allowPublicKeyRetrieval=true&useSSL=false` |
| K6 | 中文字符集 | 数据库里中文乱码 | 库/表 `utf8mb4`，连接串 `characterEncoding=utf8` |
| K7 | 时间字段 | MySQL 8 时区导致 `publishTime` 偏移 | 连接串 `serverTimezone=Asia/Shanghai` |
| K8 | 端口占用 | MySQL 3306 被占 | `init_check.py` 启动前检测端口 |
| K9 | **CSV 读取编码** | 中文列名乱码 | 最终数据集是 **UTF-8 with BOM**，读取时用 `utf-8-sig`（Python）/ 指定 `encoding=UTF-8` 并处理 BOM（Spark） |
| K10 | **CSV 大字段与换行** | 行数错乱 / 字段错位 | 评论正文含全角逗号与长文本，读取时需 `multiLine=true` + `escape='"'`，并在清洗层做**行数校验**（必须等于 59,033） |
| **K11** | **PySpark 与 Python 版本** | `PySpark 3.3.1` 在 Python 3.11 下可能报兼容错误 | PySpark 3.3.1 官方验证到 **Python 3.10**。若报错，用 venv/conda 建 **Python 3.10** 环境跑 PySpark；或**用 Scala 写 Spark 作业**（推荐，你有 Scala 2.13.10 + IDEA + Maven 环境，更贴合答辩印象）。纯 Python 的清洗与 DeepSeek 批处理仍用 3.11.7 |

### 8.5 MySQL 表结构清单（初稿，详细设计文档中给完整 DDL）

| 类别 | 表名 | 用途 | 主键 / 唯一键 |
|---|---|---|---|
| 清洗 | `review` | 评论明细（59,033 条，含规范化字段与情感/方面外键） | `comment_id` |
| 清洗 | `spot` | 景点维表（837 条，来源口径、地址、介绍、开放时间、电话） | `spot_name` |
| 清洗 | `clean_log` | 清洗步骤日志（处理量/丢弃量/异常量） | 自增 |
| 统计 | `stat_spot` | 景点级全量指标 | `spot_name` |
| 统计 | `stat_time` | 时间维度指标（年/月/季度） | `(period_type, period)` |
| 统计 | `stat_ip` | 客源地指标（限定 2022-08 后口径） | `ip_province` |
| 智能 | `sentiment` | 三路情感结果并列（DeepSeek / MLlib / 词典法） | `(comment_id, method)` |
| 智能 | `aspect` | 方面级结果（方面名、极性、证据句） | `(comment_id, aspect_name)` |
| 智能 | `topic` / `topic_word` | 主题与主题词分布 | `topic_id` |
| 智能 | `spot_report` | 景点智能评价报告（事实包快照 + 生成文本 + 引用依据） | `spot_name` |
| 运维 | `ml_model` | MLlib 模型持久化记录（版本、路径、训练指标） | `model_version` |
| 运维 | `user` | 系统用户与角色 | `user_id` |
| 运维 | `task_log` | 离线任务运行日志（可追溯、可断点） | 自增 |

---

## 九、风险与预案

| # | 风险 | 影响 | 预案 |
|---|---|---|---|
| R1 | 单景点样本严重长尾（中位 5 条） | "景点级评价"结论不稳 | 分级降级规则（<10 条不出评价；≥100 条出完整报告） |
| R2 | 评分 5 星占 71.7% | 分类/回归指标虚高 | 报告类别分布 + 分层评估 + 只把回归当辅助 |
| R3 | IP 属地 2022-08 前 100% 缺失 | 客源分析失真 | 硬编码 2022-08 后口径并在界面标注 |
| R4 | 评论中位仅 31 字、17.3% ≤10 字 | 方面抽取召回低 | 低信息量过滤 + 显式报告样本量 + 主题建模去重开关 |
| R5 | 1,285 组重复正文（7.18%） | 主题分布被短句污染 | 保留统计口径 / 建模剔除，双口径并存并在论文说明 |
| R6 | DeepSeek 批处理成本与耗时 | 全量 5.9 万条调用成本不可控 | 分层策略（≤10 字走规则）+ 结果落库 + 断点续跑 + 文本缓存去重 + 限并发（§7.4） |
| R7 | 大模型"一本正经胡说" | 评价结论不可信 | 事实包约束生成 + 输出必须引用数据 + 人工抽查 |
| R8 | 数据口径被质疑（"西藏"vs"西藏+沿线"） | 答辩风险 | 全程统一表述 + 提供西藏-only 31,752 条版本对照 |
| R9 | 时间序列不完整 | 趋势结论被质疑 | 定位为"平台评论量分布"，不称客流量，不给预测承诺 |
| R10 | **Python 与 Spark 职责被人为重复** | 被质疑"为了用 Spark 而用 Spark" | 明确分工：Python 做行级清洗与文本预处理，Spark 做全量聚合与模型训练（§7.1 架构纪律 1） |
| R11 | **PySpark 在 Python 3.11 下不兼容** | 作业跑不起来 | K11：改用 Scala 版 Spark 作业（推荐）或建 Python 3.10 环境 |
| R12 | JDK1.8 环境坑位导致跑不起来 | 演示失败 | §8.4 十一条坑位清单 + `init_check.py` 启动前自检（Python/JDK/Spark/端口/驱动/表结构） |
| R13 | 全链路重跑导致结果不一致 | 结论不可复现 | 每步写 `task_log`；固定随机种子；DeepSeek 结果落库不重复调用（断点续跑） |

---

## 十、需要你确认的事项（确认后进入开题报告撰写）

**已确认**：
- ✅ **技术路线（指导老师确认）**：Python 数据清洗 + Spark 离线分析 + DeepSeek 智能评价与总结，**不做实时分析**
- ✅ 技术环境：JDK1.8 / Scala2.13.10 / Spark3.3.1 / MySQL8 / Navicat / Python / IDEA2024
- ✅ MySQL `localhost:3306`，口令见本地 `.env`（不写入文档、不提交仓库）
- ✅ 系统架构：四层离线架构（清洗 → 离线分析 → 智能评价 → Web 展示）
- ✅ 不采用：Flume / Kafka / Spark Streaming / 实时分析 / 个性化推荐

**仍需你确认 4 件事**：

1. **数据口径命名**：是否采用"**西藏及进藏沿线（川藏/滇藏/青藏线）旅游景点评论数据集**"？纯西藏口径只有 31,752 条 / 554 景点。
2. **景点分析门槛**：是否采纳"**≥100 条的 57 个景点出完整智能评价报告，<10 条只出统计、不出评价**"的分级策略？
3. **前端**：确认 **HTML + ECharts（Jinja2 + axios）**，还是你希望用 **Vue3**？（Vue 会显著增加工作量，且需 Node 构建环境）
4. **DeepSeek 接入方式**：使用 **DeepSeek 官方 API**（需 API Key、按量计费、效果最好），还是**本地部署 DeepSeek 蒸馏模型**（免费但需算力、效果与稳定性下降）？这直接决定 §7.4 的实现与实验设计。

> 若第 4 项尚未确定，可先按 **DeepSeek 官方 API** 设计（接口层做抽象，后续可替换为本地模型），并在论文中说明该可替换设计。
> **另需你补充一个信息**：你更希望 Spark 作业用 **Scala** 写（贴合你的 Scala 2.13.10 + IDEA + Maven 环境，答辩印象更好）还是用 **PySpark** 写（Python 一套到底，但受 K11 版本约束）？我的建议是 **Scala 写 Spark、Python 写清洗与 DeepSeek 批处理**，两者通过 MySQL 解耦。



---

## 附：本阶段实测数据出处

| 结论 | 数据来源 |
|---|---|
| 59,033 行 / 15 列 / 837 景点 / ID 唯一率 100% | 全量扫描 `旅游评论数据集_最终版.csv` |
| 评分分布、长度分布、点赞/图片、年份、客源地 | 同上（逐行统计） |
| 景点条数分档、Top 景点、≥100 条 = 57 个 | 同上 |
| 西藏 554/31,752、沿线 283/27,281 | `景点来源标注.csv` |
| 1,285 组 / 4,239 条 / 跨景点 759 组 | `内容重复清单.csv` |
| IP 属地 2022-08 前 100% 未知 | 全量扫描 + 按年交叉统计 |
| 清洗链路与字段映射 | `爬虫工具/ctrip_tools.py`（`strip_tags`/`unescape_js`/`parse_publish_time`/`extract_content`/`mode_quality`） |
| 采集过程、硬限制、交付清单 | `采集工作报告.md`、`最终数据质量报告.md`、`爬虫工具/README_使用说明.md`、`第一批复盘与后续计划.md` |
| 版本演进与快照 | `爬虫工具/data/out/阶段快照/`（15 份） |
